# experiments/ — 非维护实验区

本目录是历史/探索用 notebook 与脚本（飞书/GitHub/语雀 API 摸底），**不属于 OasisMind 运行时或 CI 验收范围**。

- 不要把生产逻辑依赖这里的代码。
- 正式能力已进 `apps/server`（native tools / channel adapters）。
- 可随时删除；需要时以开放平台文档为准重做。
