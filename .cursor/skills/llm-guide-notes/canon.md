# 行文样本（先读再写）

写新笔记前至少精读 **用户点名的两篇 + 下面「加选」里与本篇同族的一篇**。不要只看本摘要。

路径均相对仓库根 `content/llm-guide/`。

## 用户点名（风格锚）

### 1. MoE 长文（结构 / 标题拆分 / 机制深度）

`2-核心原理与架构/2.4-前沿架构与变体/2.4.1-混合专家模型MoE/2.4.1-混合专家模型MoE.md`

学什么：

- 大标题下按 **思想起源 → 条件计算 → 路由 → 负载均衡 → 实现分叉 → 实证** 拆节，每一节写满一个机制。
- 公式跟在「这件事在算什么」后面：门控 $g_i(x)$、Top-K 后再 Softmax、辅助损失 $f_i \cdot P_i$。
- 对比写进正文：Token Choice vs Expert Choice；「先 Top-K 再 Softmax」vs 先 Softmax 再截断（点名 Qwen / DeepSeek V1–V2）。
- 引用论文用可点链接（Switch Transformers、OLMoE），数字跟实验走。

不要学（旧病，新文禁止再犯）：

- 语雀 / Substack 外链图。新文一律本地 `./images/fig-*.png`。
- 文首「终极指南 / 参数军备竞赛」营销腔。改成 MHA/RoPE 那种 2–5 句破题。

### 2. MHA 标准形式（教材骨架 / 图解析 / 系列零点）

`2-核心原理与架构/2.2-基础注意力机制/2.2.2-多头注意力变体/01-MHA-多头注意力的标准形式/01-MHA-多头注意力的标准形式.md`

学什么：

- 开篇立刻定义算子，并声明本文是 **KV Cache 参照系**，供 MQA/GQA/MLA 对比。
- 本地图 + `> 图 N：…（Vaswani et al., 2017, Figure 2）` + **图 N 解析**（自下而上：MatMul → Scale → Mask → Softmax）。
- 矩阵式与坐标展开分层；因果掩码写进 softmax 之前。
- 结尾指向下一篇相对路径，不把整条演进写完。

标题形态：`01-MHA-多头注意力的标准形式`（序号 + 缩写 + 中文机制名）。

## 加选高质量（同库、同笔法）

写注意力变体先读 3、4、5；写位置编码读 6；写第 14 章厂商精读读 7；写训练系统/激活读 8。

| # | 路径 | 学什么 |
|---|------|--------|
| 3 | `…/2.2.2-多头注意力变体/03-GQA-在性能与缓存之间折中/03-GQA-在性能与缓存之间折中.md` | 「沿用 MHA/MQA 记号」；$G=H$/$G=1$ 插值；图解析对着色块；性能–延迟散点怎么读 |
| 4 | `…/2.2.2-多头注意力变体/02-MQA-共享KeyValue的极致压缩/02-MQA-共享KeyValue的极致压缩.md` | 同一系列的极致压缩端点 |
| 5 | `…/2.2.2-多头注意力变体/05-MLA矩阵吸收与非吸收双版本/05-MLA矩阵吸收与非吸收双版本.md` | 一篇只回答一个工程问题（Prefill/Decode 两套图是否代数等价）；「不是」写清楚 |
| 6 | `2-核心原理与架构/2.1-深度学习基础组件/2.1.4-位置编码/01-RoPE本体-旋转位置编码.md` | 科学空间节奏：破题一句（相对位置在点积里出现）→ 式 (1)(2)(3) → 扩展另篇 |
| 7 | `14-主流开源模型全景解析与技术报告精读/14.1-DeepSeek/05-DeepSeek-V3/05-DeepSeek-V3-MLA.md` | 第 14 章精读：先算 MHA KV 字节再引入压缩；配置数字带模型尺寸 |
| 8 | `6-训练与推理优化/6.1-训练基础设施/6.1.1-分布式训练/6.1.1-分布式训练.md` **§4.7 BPT** | 薄项怎么写：论文号、一层激活 $2bsh$、Table 同行数字、明确 **不是** Ring/FA/SP |

外站节奏（读风格不抄图）：[科学空间 · RoPE](https://kexue.fm/archives/8265) 对应 RoFormer [arXiv:2104.09864](https://arxiv.org/abs/2104.09864)。

## 骨架对照（可复制）

```markdown
---
title: "0N · 缩写：中文机制名"
date: 2026-08-30
tags: [缩写, …]
as_of: 2026-08-30
---

# 0N 缩写：中文机制名

{2–5 句：对象 + 瓶颈 + 本篇在系列中的位置。禁止家谱散文。}

## 1. {具体问题}

{已有做法差在哪，最好带数量级。}

$$
\ldots \tag{1}
$$

![](./images/fig-….png)

> 图 1：….（论文 Figure k）

**图 1 解析**

- …

## 2. {机制 / 公式}

## 3. {工程变体或和邻居的「不是」}

## 4. {失效模式}

| 现象 | 原因 | 说明 |
|------|------|------|

下一篇：[…](相对路径)

## 本篇来源

1. 作者. (年). [题](arxiv). *会议*.
```

## 反例（看见就改）

- 语雀/Substack 图、水印、Logo、URL 角标。
- **深色主题配图**（黑/藏青底、白字、OLED 幻灯片）。必须白底深字。
- `## 标题` 下面只有一句或一个列表。
- 空壳 D2/D5、Index 把「文件存在」标成 completed。
- 把 FlashAttention、Memory Efficient Attention、BPT、Ring、序列并行揉成一篇。
- `> **2026-08 修订（不删上文）` 与错段并排。读者只能面对一版完整正文。
- 第 14 章厂商目录里的 trick 不反链回第 2/6/9 章本体。
