---
title: "LLaDA：8B 从头训到 100B 改编"
category: null
tags:
  - LLaDA
  - LLaDA2.0
  - MoE
  - frontier
published: true
as_of: 2026-08-31
excerpt: "LLaDA 8B 用 2.3T token 从头做掩码扩散，Base 的 MMLU 5-shot 65.9 对 LLaMA3 8B 的 65.4。2.0 不再从头训 100B，而是把 AR MoE 经三阶段块级 WSD 转成扩散。数字全部来自论文表，旧稿那组 62.3 / 54.8 已作废。"
---
# LLaDA：8B 从头训到 100B 改编

LLaDA 要回答的问题很硬：LLM 的规模化、上下文学习、指令跟随，是不是必须绑在自回归连乘上。作者的答案是：这些能力来自「用似然（或其下界）拟合语言分布」，不来自「必须从左到右」。8B 从头训是把这句话做到和 LLaMA3 8B 同一张表上；2.0 是承认从头训 100B 太贵，改成继承 AR 权重。

机制公式见掩码扩散篇。本篇钉架构选择、表上的数字、采样器对分数的影响、以及 2.0 的转换课程。后训练（VRPO、diffu-GRPO）不在 8B 原论文主表里，文末只给指针和对照口径。

## 1. 8B 骨架：去掉因果掩码的 decoder-only

预训练 2.3T token，0.13 百万 H800 GPU 小时，序列长 4096。损失即掩码扩散篇式 (1)：抽 $t\sim U[0,1]$，每个位置独立以概率 $t$ 变成 `[MASK]`，交叉熵只落在被掩位置上，再乘 $1/t$。SFT 用 450 万对，只掩回答，提示保持干净。没有 RL。

和 LLaMA3 8B 的对照写在附录 Table 5：都是 32 层、隐维 4096、32 个注意力头。LLaDA 用 32 个 KV 头（vanilla MHA），LLaMA3 用 8 个（GQA）。作者写明原因：当时这套全双向公式和 KV Cache 不兼容，GQA 省 KV 的前提不成立。多出来的注意力参数靠减小 FFN（12288 对 14336）对齐到 8.02B / 8.03B。词表 126464 对 128000，分词器按他们的数据改编。

不要把「BERT + timestep」理解成换了一套完全不同的层。换掉的是 mask 和训练目标。RoPE、SwiGLU、RMSNorm 仍在。BERT 的 15% 固定掩码、NSP、10% 随机替换都不在。$t$ 要从 0 积到 1，生成要从全掩码走回来，这两件事才把编码器接上生成模型。

1% 的预训练样本会把长度在 $[1,4096]$ 里另抽一次，避免模型只会处理垫满 4096 的块。生成长度在纯扩散里仍是超参：一开始垫多少个 `[MASK]`。Instruct 数据若用 `[EOS]` 垫到固定长，纯扩散会学会到处写结束符，采样时要把 `[EOS]` 置信度置零。这不是 $Q_t$ 的问题，是定长垫片和结束符抢置信度。

训练一步长什么样。抽一条 4096 的文档，抽一个 $t$，抛硬币决定哪些位置变成 `[MASK]`。网络做一次全双向前向，词表 softmax 只在被掩位置上对原 token 算交叉熵，乘 $1/t$ 再反传。未掩位置的答案已经写在输入里，再预测一次没有信息，还会让模型学抄输入。这和 BERT 的差别不在「用了双向 Transformer」，而在 $t$ 覆盖 $[0,1]$ 全体：有的 batch 几乎没掩码，有的 batch 几乎全掩。似然下界是对这条日程求期望，所以可以从全掩码反向采样。固定 15% 的 MLM 没有这条反向过程。

SFT 把同一套损失收窄到回答段。提示位置永远可见，当作条件；回答位置按 $t$ 掩。模型学的是「在这段已经写好的指令旁边，把回答从噪声里擦出来」，不是「从左到右续写指令」。这解释了为什么同一组 Instruct 权重，用自回归采样（每步只揭最左边一个掩码）会和 `[EOS]` 垫片打架，而纯扩散、块扩散还能出分数。

同数据 ARM baseline 是作者用来挡「2.3T 对 15T」的那条曲线。语料配比、分词器、优化器尽量对齐，只换因式分解。下游在他们走到的 FLOPs 区间没有塌。这张图回答的是「扩散目标能不能 scale」，不是「LLaDA 的数据比 LLaMA3 更省」。跨公司主表上的 GSM8K 21 个点，混了数据、目标和双向，不能单独记在扩散名下。

## 2. Base 表：同一评测协议下的 LLaMA3

Table 1 是预训练模型。带 $*$ 的 LLaDA 8B 与 LLaMA3 8B、LLaMA2 7B 是作者同一套协议重测的。摘几列（括号内为 shot）：

| 任务 | LLaDA 8B Base | LLaMA3 8B Base | LLaMA2 7B Base |
|---|---|---|---|
| 训练 token | 2.3T | 15T | 2T |
| MMLU (5) | 65.9 | 65.4 | 45.9 |
| BBH (3) | 49.7 | 62.1 | 39.4 |
| GSM8K (4) | 70.3 | 48.7 | 13.1 |
| MATH (4) | 31.4 | 16.0 | 4.3 |
| HumanEval (0) | 35.4 | 34.8 | 12.8 |
| MBPP (4) | 40.0 | 48.6 | — |
| Hellaswag (0) | 70.5 | 79.1 | 76.0 |

MMLU 与 HumanEval 和 LLaMA3 持平量级；GSM8K / MATH 高出一截；BBH、Hellaswag 落后。作者把优劣同时归因于闭源数据不可比，并另外训了同数据的 ARM baseline，在 $10^{20}$–$10^{23}$ FLOPs 上显示下游曲线可以跟上，MMLU 与 GSM8K 甚至更陡。讨论「扩散样例效率」用那条同数据曲线，不要用 2.3T 除以 15T。

旧稿写 MMLU 62.3 / GSM8K 54.8 / HumanEval 38.4，对不上 Table 1–2，以本表为准。Dream 7B 论文 Table 1 也列了 LLaDA 8B，但 shot 与协议不同（GSM8K 写成 8-shot 的 70.9，HumanEval 写成 32.9），不能和本表混成一列。

Base 的 MMLU 类任务走条件似然蒙特卡洛，HumanEval 走生成。两列不是同一种解码。HumanEval-FIM 在 Table 1 上是 73.8 对 LLaMA3 的 73.3：填空不是扩散独有的魔法，FIM 训过的 AR 也能做；扩散这边不必另开一条 FIM 目标。

附录 Table 10 把生成长度扫过 256 / 512 / 1024。Base 上 GSM8K 在 70.0–70.8 之间晃，MATH 30.3–31.4，HumanEval 32.9–35.4。定长超参不是分数的主因；采样器类别才是。本表 MATH 31.4、HumanEval 35.4 与 Table 10 在 length=1024 那一行对齐。

## 3. Instruct 表：只做了 SFT

Table 2 的对照对象大多有 SFT+RL，LLaDA 只有 SFT。

| 任务 | LLaDA 8B Instruct | LLaMA3 8B Instruct |
|---|---|---|
| 后训练 | SFT | SFT+RL |
| MMLU (5) | 65.5 | 68.4 |
| GSM8K (4) | 69.4 | 78.3 |
| MATH (0) | 31.9 | 29.6 |
| HumanEval (0) | 49.4 | 59.8 |
| MBPP (4) | 41.0 | 57.6 |
| ARC-C (0) | 88.5 | 82.4 |

整体略落后有 RL 的 LLaMA3 Instruct，缺口在代码和 GSM8K 更明显。ARC-C 反而更高。作者把若干指标相对 Base 下降（如 MMLU）归因于 SFT 数据质量，并明确把 RL 对齐留到后续。

后续工作里 LLaDA 1.5 用 VRPO 做偏好优化：相对 Instruct，GSM8K 78.6→83.3（+4.7），HumanEval 49.4→52.4，IFEval 62.2→66.2，Arena-Hard 10.0→14.3。d1 用 s1K SFT 再加 diffu-GRPO，0-shot、生成长度 512 时 GSM8K 从他们自己的基线 78.2 到 82.1。两篇的基线采样器和原论文 Table 2 的 4-shot 纯扩散 69.4 不是同一格。公式和方差分析见[对齐与强化学习](../03-points/alignment-rl.md)。

## 4. 采样器能改分数

附录 Table 8，Instruct，块长 32：

| 采样 | GSM8K | MATH | HumanEval |
|---|---|---|---|
| 自回归 | 0 | 9.5 | 0 |
| 块扩散 | 24.6 | 23.5 | 17.1 |
| 块扩散 LLaDA（半自回归 remask） | 77.5 | 42.2 | 46.3 |
| 纯扩散 | 69.4 | 31.9 | 49.4 |

Table 2 的 69.4 / 31.9 / 49.4 是纯扩散。GSM8K 若改用块长 8 的块扩散 LLaDA，可到 78.6。报「LLaDA Instruct 的 GSM8K」而不写采样器，数字没有意义。自回归采样在 Instruct 上崩掉，和 `[EOS]` padding 有关，见采样篇。

Table 9 把 remask 钉死：随机 remask 的 GSM8K 是 21.3，低置信 remask 是 70.0，生成长度设定相同。随机策略会把已经很尖的位置盖回去，同时放过仍然平的位置。低置信策略把计算预算留给还没想清楚的格子。这是 MaskGIT 式启发式，不是 ELBO 推出来的。

Table 7 在 Base 上做同类消融。自回归采样：BBH 38.1、GSM8K 63.1、Math 23.6、HumanEval 18.3、MBPP 33.4。纯扩散把 HumanEval 拉回 35.4，整体最好。块扩散采样介于两者之间，块长 $L'$ 是旋钮。同一组 8B 权重，采样器可以把它变成三种不同的模型。Instruct 上自回归采样崩到 GSM8K=0、HumanEval=0，Base 上只是变差、没有归零：SFT 的 `[EOS]` 垫片是崩掉的主因，不是「扩散权重不会从左到右」。

低置信 remask 和随机 remask 的差，比换块长更刺眼。Table 9 在相同生成长度下，随机 21.3、低置信 70.0（GSM8K）。随机策略忠实于前向后验，理论上更「对」，生成却差。原因是 $p_\theta$ 在不同位置上的校准不一样：有的格子上下文已经够、分布很尖；有的还在几个近义词之间平。随机盖回去会浪费已经尖的格子。采样篇用四格置信度把这件事画出来。

## 5. 诗歌反向：机制实验，不是全能声明

给定后半句补前半句。因果模型必须把「后面」改写成提示里的条件，且生成方向仍朝结束符走。掩码扩散把要补的字留成 `[MASK]`，已给的字当干净上下文，方向不存在。评测跟 Allen-Zhu 与 Li 的协议，496 对著名中文诗句，zero-shot。

Table 3 / Table 4：GPT-4o（2024-08-06）正向 82.7、反向 34.3。Qwen2.5 7B Instruct 正向 75.9、反向 38.0。LLaDA 8B Instruct 正向 48.8、反向 42.4。正向弱，作者写在 2.3T 和约 13 万 H800 小时对不上闭源语料。反向超过两者。论文要强调的是正反向落差被压平，不是古诗全面超过 GPT-4o。

附录里 GPT-4o 写错上句的例子：问「但闻人语响」接到「无可奈何花落去」；问「夜静春山空」接到「空山新雨后」。方向反了之后，它仍按往后续的习惯找一个顺口的下句。注意力侧的解释见[双向注意力与反转诅咒](../03-points/bidirectional-attention.md)。Berglund 微调实验里虚构名人正向 96.7%、反向约 0%，测的是另一件事：参数里有没有反向边。诗句表不能外推成「扩散已经解决知识对称」。

## 6. 2.0：转换，不从头堆 100B

LLaDA 2.0 从 Ling-mini-2.0 / Ling-flash-2.0 出发，把 AR 看成块大小为 1 的块扩散，再用 Warmup–Stable–Decay 改块大小。

![](./images/fig-llada-wsd.png)

> 图 1：2.0 把现成 AR MoE 当成 $B=1$，warmup 加大块并打开双向，stable 做全序列扩散，decay 再收到适合部署、能接 KV 的块。

**图 1 解析**

- **AR MoE (B=1)**：不是新训一个扩散骨架，是继承已经学好语言几何的权重。$B=1$ 在概率上就是 AR 连乘。
- **Warmup: grow block size**：块太一下子全打开，改编会把 AR 特征打乱。这一段让模型习惯块内出现 `[MASK]` 和右边上下文。
- **open bidirectional**：因果掩码在这里放宽。放宽速度是超参，对应 DiffuLLaMA 里的掩码退火。
- **Stable: full-sequence diffusion**：双向去噪能力拉满，数据利用率比小块 teacher forcing 高。主评测数字不是跑在这个设定上。
- **Decay: shrink block**：推理时太大的块又把 KV 和并行误差赔回去。flash 主结果跑在收小之后。
- **KV-friendly B**：块间前缀可以真缓存。这和 Fast-dLLM 那种跨去噪步的近似缓存不是一回事。

发布的是指令微调后的 MoE：mini 16B、flash 100B。旧稿写的「~4B / ~20B 激活」在 2.0 HTML 正文里没有并列成规格表，本篇不引用。

Table 1–2 的平均分：mini 64.34，对照 Ling-mini-2.0 的 65.77、Qwen3-8B (no_think) 的 63.42。flash 73.18，对照 Qwen3-30B-A3B-Instruct-2507 的 73.60、Ling-flash-2.0 的 72.15。flash 的 HumanEval 94.51、MBPP 88.29、AIME 2025 60.00、BFCL v3 75.43，作者强调编码与 agent 相对 AR 同级模型开始占优。

吞吐：作者用 dInfer，阈值 0.95。flash-CAP 535 TPS，普通 flash 383 TPS，SGLang 上的 AR 基线 256 与 237 TPS，最高约 2.1 倍。不要把这段写成「3–8 倍」；那是旧稿无来源的数字。CAP 是在已经预测对的位置上再压熵，提高每步提交的 token 数（TPF），不是换 $Q_t$。

上下文：文内称 32k 内 flash 稳定。训练用 Megatron 的 DP/PP/TP/CP/EP，掩码在模型并行组内广播以保持一致。文档打包必须在边界切断注意力，否则下一篇的开头会变成本篇的未来上下文，去噪时作弊。

100B 是改编不是从噪声训出来的扩散。知识来自 AR 阶段，扩散阶段负责改生成过程。讨论「扩散能否 scale」时要把「从头训的 8B」和「转换的 100B」分开。

## 7. 规模曲线和 CFG

原文 Fig. 3 用预训练 FLOPs 当横轴，六个下游任务上 LLaDA 与同数据 ARM 的曲线总体可并排。作者拒绝拟合定量幂律，理由是离群点会误导。能说的只有：在他们走到的 $10^{23}$ FLOPs 量级，扩散没有在下游上突然塌掉。早期「似然要 16 倍算力才追上 AR」的观察，被他们用「似然是间接指标、优化的是界、FLOPs 区间更宽」三句话挡回。本花园不把 16 倍写成 2025 年的换算表。

CFG 在附录 Table 6：有条件与无条件 logits 外推，多个任务稳定加分。这是推理技巧。8B Instruct 的主表没有把 CFG 当成默认必开项来报，读具体数字时看附录有没有开。Base、不开 CFG 的那一行：ARC-C 45.9、Hellaswag 70.5、TruthfulQA 46.1、WinoGrande 74.8、GPQA 25.2、PIQA 73.6。开 CFG 并在 $\{0.5,1,1.5,2\}$ 里按任务选最好的 $\gamma$ 之后：47.9、72.5、46.4、74.8、26.1、74.4。作者写明主表为了和 ARM 公平对照，不开 CFG。离散 CFG 的公式和基因组上的 γ 表见[可控生成](../03-points/controllable-generation.md)；8B 语言 CFG 没有与 Table 2 同口径、扫完整指令任务的公开细表。

读架构的人常问：8B 为什么不用 GQA。答案已经写在 Table 5 旁边：全双向下 Key 会随揭开而变，省 KV 头省的是一份每步都可能过期的缓存。后来 DualCache、延迟写入把「过期但有界」变成可卖的近似。那是 2025 年中的加速论文，不是 2 月这张 8B 检查点自带的能力。把两件事焊在一句「LLaDA 也会 KV Cache」里，读者会以为原论文的 MHA 选择是多此一举。

SFT 的 450 万对只掩回答，看起来像普通指令微调。差别在损失仍是 $1/t$ 加权掩码交叉熵，不是 next-token。模型学的是「在干净提示旁边把回答从噪声里擦出来」。所以 Instruct 权重拿去当因果模型逐步揭最左边一个掩码时，会和垫进去的 `[EOS]` 打架，GSM8K 可以掉到 0。这不是权重坏了，是解码器和训练分布不一致。块采样把结束符的传播挡在块边界，分数立刻回来。换采样器比再收集 450 万对便宜，也解释了为什么本花园把采样器写成规格的一部分。

## 8. 失效与不该推出的结论

8B Base 的 GSM8K 高于 LLaMA3 Base，推不出「扩散更会数学」。同数据 ARM baseline 才是公平对照；跨公司数据配比是混杂因素。作者自己写了这句。

没有 RL 的 Instruct 落后有 RL 的 LLaMA3 Instruct，推不出「扩散对齐不了」。只能推出：这篇论文没有做 RL。1.5 和 d1 后来补上了，口径仍要写采样器和 0-shot / 4-shot。

全双向 8B 推不出「扩散不能 KV Cache」。2.0 和 Fast-dLLM 已经走块扩散与近似缓存。8B 论文的架构选择是当时公式下的简化。Fast-dLLM 在 LLaDA-Instruct、GSM8K 5-shot、生成长度 256 上，从 6.7 tok/s 到组合加速 54.4 tok/s，约 8.1 倍；27.6 倍那一格的对照物是原版 LLaDA 循环，不是 AR。细节见[推理加速](../03-points/inference-acceleration.md)。

把诗歌反向 42.4 抄进产品总榜，推不出聊天、代码、代理全面超过 GPT-4o。正向 48.8 对 82.7 写在同一张表上。

把 2.0-flash 的 535 TPS 和 Mercury Mini 的 1109 tok/s 减出「谁快一倍」：硬件、是否含 prefill、batch、评测集都不同。本花园只并列。

## 9. 读完应留下的规格卡

- 8B Base：2.3T，MMLU 65.9，GSM8K 70.3，HumanEval 35.4（Table 1，$*$ 协议）。BBH 49.7 对 62.1，Hellaswag 70.5 对 79.1。
- 8B Instruct：仅 SFT，Table 2 纯扩散 GSM8K 69.4；块长 8 的块扩散 LLaDA 可到 78.6。
- 不兼容当时的 KV Cache，故用 MHA 不是 GQA。
- 诗句：正向 48.8 / 反向 42.4，GPT-4o 为 82.7 / 34.3。
- 2.0-mini 16B / flash 100B MoE，平均分 64.34 / 73.18；flash-CAP 535 TPS，约 2.1× 于文内 AR 基线。
- 从零稀疏另一条河：LLaDA-MoE 非嵌入 7B、激活 1.4B、约 20T，见[LLaDA-MoE](./llada-moe.md)。不要和 2.0 的改编焊。
- 激活参数、旧稿 3–8× 吞吐：2.0 正文未给出可引用的并列规格，不写。

把 8B 和 2.0 放在同一条「LLaDA 家族」里读时，记得训练起点不同。8B 的语言几何是掩码扩散自己刷 2.3T 刷出来的；100B 的语言几何主要是 Ling AR 阶段刷出来的。评「扩散能不能从噪声长出知识」，只用 8B。评「扩散能不能接到现有 MoE serving」，用 2.0。把 flash 的 HumanEval 94.51 写成「掩码扩散从零训到 100B 就会写代码」，句子里的「从零」是假的。

8B 的 13 万 H800 小时是公开数字里很少见的「从头训扩散 LLM 要多少钱」。改编路线存在，就是因为这个数字劝退了 100B 从头训。读 2.0 时把这笔账放在分子里：转换课程再精巧，也是在逃避这笔账，不是把这笔账变便宜。逃避得对不对，看 flash 平均分 73.18 对 Qwen3-30B-A3B 的 73.60 那一格——同档，不是碾压。碾压叙事来自旧稿无来源的 3–8 倍吞吐，已经删。

旧笔记里那组 MMLU 62.3 / GSM8K 54.8 / HumanEval 38.4 从哪来的，本篇不再考古。能核对的只有 Table 1–2 和附录消融。读二手综述时若看见这组数，当它过期。同样过期的还有「LLaDA 8B 推理快 2–8 倍」：原论文没有速度表。速度去 Fast-dLLM、dInfer、Mercury 那些专门测吞吐的工作里找，并且写清对照物。

HumanEval-FIM 73.8 对 LLaMA3 的 73.3 说明填空可以被 AR 用数据增强买到。扩散买到的是不必另开 FIM 模板：任意一组位置同时是空位，不必是连续 middle。产品若只有「函数中间缺一块」，FIM 够用；若表格缺不相邻的几格，掩码扩散少一层改写。这格不要用来宣传「代码全面超过」，两数几乎打平。

规格卡再补一条读法。8B Base 的 GSM8K 70.3 是生成，MMLU 65.9 是似然估计，不要合成「综合分」。Instruct 的 69.4 是纯扩散，78.6 是块采样，不要合成「指令分」。2.0 的 535 TPS 对照文内 SGLang AR，不要对照 Mercury Mini。三条读法挡住了本花园写错过的三类旧稿。

CFG 的 Table 6 还说明一件评测纪律。主表不开 CFG，是为了和 ARM 公平。开了之后 Hellaswag 从 70.5 到 72.5，仍低于 LLaMA3 的 79.1。引导能加分，加不回数据量和因式分解一起造成的那截常识续写差距。把 CFG 写成「扩散的秘密武器，一开就全面超过」，Table 6 自己不支持。

8B 的同数据 ARM baseline 才是「扩散目标能不能 scale」的证据。跨公司的 2.3T 对 15T 混了语料。Fig. 3 用 FLOPs 当横轴，作者拒绝拟合幂律，只说走到 $10^{23}$ 量级下游没有突然塌掉。读规模故事时用这条曲线，不要用「LLaMA3 看了更多 token 所以应该全面领先」或「扩散用更少 token 所以更高效」。两句都缺同配比。Table 1 能说的是：在他们的数据和公式下，8B 扩散可以和当时的开源 8B AR 坐同一张下游表。坐上桌子，不等于每一格都赢。BBH 49.7 对 62.1、Hellaswag 70.5 对 79.1，同一张表上写着。

SFT 450 万对只掩回答，损失仍是 $1/t$ 加权掩码交叉熵。模型学的是「在干净提示旁边把回答从噪声里擦出来」。所以 Instruct 权重拿去当因果模型逐步揭最左边一个掩码时，会和垫进去的 `[EOS]` 打架，GSM8K 可以掉到 0。这不是权重坏了，是解码器和训练分布不一致。块采样把结束符的传播挡在块边界，分数立刻回来。换采样器比再收集 450 万对便宜。规格卡因此把采样器写成规格的一部分，而不是脚注。

## 参考文献

- [Nie et al., LLaDA, 2025](https://arxiv.org/abs/2502.09992) — Table 1–2、5、7–10；2.3T；反转诗歌。
- [Bie et al., LLaDA 2.0, 2025](https://arxiv.org/abs/2512.15745) — WSD；mini/flash 表；535 TPS。
- [Zhu et al., LLaDA 1.5, 2025](https://arxiv.org/abs/2505.19223) — VRPO 偏好优化，不在 8B 原表。
- [Zhao et al., d1, 2025](https://arxiv.org/abs/2504.12216) — diffu-GRPO。
- [Wu et al., Fast-dLLM, 2025](https://arxiv.org/abs/2505.22618) — 相对原版 LLaDA 的吞吐，不是相对 AR。

## 相关

- [掩码扩散](../02-mechanism/masked-diffusion.md)
- [采样与调度](../02-mechanism/sampling.md)
- [块扩散](../03-points/block-diffusion.md)
- [从自回归改编](../03-points/ar-to-diffusion.md)
- [对齐与强化学习](../03-points/alignment-rl.md)
- [代表性工作](./representative-models.md)
- [多模态扩散](./multimodal-dllm.md)
- [SDAR](../03-points/sdar.md)
- [APD](../03-points/apd.md)
- [LLaDA-MoE](./llada-moe.md)
- [Serving](../03-points/serving.md)
