---
title: 4.1 经典 TTT 与自监督适应
category: 测试时适应
tags: [classic-ttt, self-supervision, distribution-shift]
published: true
excerpt: 精读经典 Test-Time Training 的联合训练、测试时更新与适用边界。
---

# 4.1 经典 TTT 与自监督适应

Sun 等人在 ICML 2020 的 Test-Time Training（TTT）中，让主任务与自监督辅助任务共享特征提取器。测试时没有主任务标签，但可以从输入自身构造辅助监督，先更新共享特征，再进行主任务预测。

## 训练阶段

设特征提取器为 $f_\theta$，主任务头为 $g_\phi$，自监督头为 $h_\psi$。训练目标可概括为：

$$
\mathcal{L}_{train}=\mathcal{L}_{main}(g_\phi(f_\theta(x)),y)
+\lambda\mathcal{L}_{self}(h_\psi(f_\theta(\tilde{x})),\tilde{y}).
$$

原论文在图像任务中使用旋转预测作为自监督信号。辅助任务必须在训练阶段就与主任务共同塑造表示；不能随意在测试时加一个从未联合训练过的辅助头并期待等价效果。

## 测试阶段

对无标签目标输入构造自监督样本，执行一个或多个梯度步骤：

$$
\theta' \leftarrow \theta-\eta\nabla_\theta\mathcal{L}_{self}(x),
$$

再用更新后的特征与主任务头得到预测。主任务标签不参与适应。

## 成功所需条件

1. 自监督损失在目标数据上可计算。
2. 辅助任务与主任务需要共享有用的变化方向。
3. 适应步数和学习率不能把模型推离可泛化区域。
4. 评价必须说明每个样本或批次后是否重置参数。

如果辅助任务与主任务失配，降低自监督损失可能不会改善主任务，甚至造成负适应。

## 与相邻路线的区别

- **TENT**：不要求训练时预设自监督头，测试时通过预测熵适应归一化相关参数。
- **持续测试时适应**：状态沿不断变化的目标流保留，需要处理误差累积和遗忘。
- **TTT Layers**：把内循环模型的参数用作序列隐藏状态，核心目标是长上下文建模，不是对预训练分类器做域适应。

## 复现检查表

- 主任务与辅助任务分别是什么？
- 共享并更新哪些参数？
- 每个测试样本或批次执行多少步，是否重置？
- 批次组成是否泄漏域边界或未来样本？
- 是否同时报告适应前、适应后与源域保持性能？
- 是否与相同计算预算的无适应和简单 TTA 基线比较？

## 参考资料

- [Test-Time Training with Self-Supervision for Generalization under Distribution Shifts](https://proceedings.mlr.press/v119/sun20b.html)
- [论文 PDF](https://proceedings.mlr.press/v119/sun20b/sun20b.pdf)
- [官方代码](https://github.com/yueatsprograms/ttt_cifar_release)
