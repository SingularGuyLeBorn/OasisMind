---
title: "JustRL：固定配方的单阶段 GRPO 基线"
category: "强化学习与对齐算法"
tags: [JustRL, GRPO, RLVR, arXiv-2512.16649]
published: true
excerpt: "按 arXiv:2512.16649 还原 JustRL：标准 veRL GRPO、binary outcome reward、单阶段固定超参数与 clip-higher，而不是无裁剪 REINFORCE。"
---
# JustRL：固定配方的单阶段 GRPO 基线

> 证据状态：arXiv:2512.16649 v1（2025-12-18）；论文发布模型与代码。本地未独立复现，旧 dummy log-prob 实现已隔离。

JustRL 的“简单”指减少训练阶段切换和动态干预，不是发明一个无裁剪的新优化器。论文明确使用 **veRL 的标准 GRPO 实现**、二元结果奖励和 DAPO 的轻量规则验证器。

## 1. 保留与移除的内容

| 设计 | 论文设置 |
|---|---|
| 优化器 | veRL 默认 GRPO |
| 奖励 | binary outcome reward，DAPO 规则验证器 |
| 训练阶段 | 单阶段连续训练 |
| 超参数 | 全程固定，两种 1.5B backbone 共用 |
| 数据 | DAPO-Math-17k，不做离线难度过滤或在线动态采样 |
| 长度 | 最大上下文 16K，不加入显式长度惩罚 |
| KL / entropy loss | 均不使用 |
| 裁剪 | 保留 clip-higher，比例范围 `[0.8, 1.28]` |

因此旧稿的“No PPO clipping”“直接用 REINFORCE + group norm”“group size 64+”都不是论文配方。论文表 2 的 rollout N 是 8，学习率为固定 `1e-6`，训练 batch 256。

## 2. 实验范围

论文在 DeepSeek-R1-Distill-Qwen-1.5B 与 OpenMath-Nemotron-1.5B 两个 backbone 上训练，使用 32 张 A800-80GB，约 15 天；同一组超参数分别训练 4,000+ 与 3,000+ steps。作者在九个数学基准上报告约 54.9% 与 64.3% 的平均准确率，并把总计算与若干复杂 recipe 比较。

这些结论属于两个 1.5B 数学推理设置。不能外推为“单卡可训练 7B”“PPO 普遍有害”或“所有稳定性机制都不需要”。论文真正支持的是：在其规模和协议里，一个经过验证的固定基线可以长时间平滑扩展。

## 3. 为什么移除旧实现

旧 `implementation.py` 用 `torch.randn(..., requires_grad=True)` 伪造 log-prob，梯度与模型参数无关，同时删掉了论文实际使用的 importance ratio 和 clip-higher。它已与 Notebook、无关 LIMA PDF 一起移入 `_archive/unverified/19_JustRL-错误旧稿/`，不再出现在公开算法目录。

## 4. 一手来源

- [JustRL: Scaling a 1.5B LLM with a Simple RL Recipe](https://arxiv.org/abs/2512.16649)
- [arXiv HTML v1：训练设置与固定超参数](https://arxiv.org/html/2512.16649v1)
- [作者官方代码仓库](https://github.com/thunlp/JustRL)
