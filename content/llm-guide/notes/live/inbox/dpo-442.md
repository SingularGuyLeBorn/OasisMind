---
title: inbox · dpo-442
date: 2026-08-31
published: false
---

# dpo-442 回传

租约：只改 `content/llm-guide/4-后训练/4.4-对齐技术/4.4.2-无奖励模型的对齐DPO-KTO/01-DPO/` 与本文件。未改 GOAL/PLAN/PROCESS、未改 Skill、未改 supervisor、未改 `4.4.2-….md` 节首页、未改 `02-ORPO` / `03-KTO` / `04-SimPO`、未改夹里旧路径 `02-DPO深度解析：…`、未改 `apps/`。未 Delete、未 commit、未 push、未 `git add -A`、未 `move_agent_to_root`。

## 落点

- `content/llm-guide/4-后训练/4.4-对齐技术/4.4.2-无奖励模型的对齐DPO-KTO/01-DPO/01-DPO.md`（整篇覆盖重写）
- `.../01-DPO/images/fig-dpo-vs-rlhf-rm.png`
- `.../01-DPO/images/fig-dpo-implicit-reward.png`

## 一手 URL（已开 HTML）

| 题目 | URL | 写进 |
| --- | --- | --- |
| Rafailov et al. DPO 2305.18290 | https://arxiv.org/abs/2305.18290 · https://arxiv.org/html/2305.18290 | 式 (3) KL 约束 RLHF；式 (4) 最优策略闭式；式 (5) $r=\beta\log(\pi/\pi_{\mathrm{ref}})+\beta\log Z(x)$；$Z(x)$ 成对抵消；BT 式 (1)；损失式 (7)；梯度 $\sigma(\hat r_l-\hat r_w)$；附录 B $\beta=0.1$/TL;DR $\beta=0.5$、batch 64、RMSprop $1\mathrm{e}{-6}$、warmup 150；IMDb GPT-2-large、25000 前缀×4 续写、6 对、siebert 分类器；TL;DR GPT-J、温度 0 胜率约 61% vs PPO 57%；人对人 DPO 0.25 vs PPO 0 为 58%；Table 1 CNN/DM 0.36/0.31 vs 0.26/0.23；HH 170k、Pythia-2.8B、唯一高于 test chosen；Table 2 人评 $N$ 与 GPT-4 (S)/(C)；Table 3 Unlikelihood 崩成 when；gpt-4-0314 |
| NeurIPS 2023 会议摘要 | https://proceedings.neurips.cc/paper_files/paper/2023/hash/a85b405ed65c6477a4fe8302b5e06ce7-Abstract-Conference.html | 会址；公式仍以 arXiv HTML 为准 |
| TRL DPO Trainer | https://huggingface.co/docs/trl/en/dpo_trainer | 实现旁注：Trainer 在算对数比进 $\log\sigma$，非公式源 |

## 汉字

专文去 YAML 后 `[\u4e00-\u9fff]`：**4047**（≥4000）。H1「01 DPO：隐式奖励直接优化」汉字 8（≤20）。`as_of: 2026-08-31`。文末「参考文献」。无空标题。无「终极指南」。

## 图

浅色、正交接线、`fig-qsa-hybrid-slot.png` 作 reference。图 1：左列 SFT→RM→PPO 四模型，右列偏好对进 DPO（策略+参考）、无独立 RM。图 2：隐式奖励差 → BT $\sigma$ → DPO 损失，$Z(x)$ 成对抵消。

## 质检

- 闭式最优策略 + 反解 $r(x,y)=\beta\log(\pi/\pi_{\mathrm{ref}})+\beta\log Z(x)$；$Z$ 在成对差里消掉。
- 损失钉论文式 (7) 的 $-\mathbb{E}[\log\sigma(\cdots)]$。
- 明确不是 SimPO（无 $\pi_{\mathrm{ref}}$，长度平均减 $\gamma$）/ KTO（不成对，$z_0=\mathrm{KL}(\pi_\theta\Vert\pi_{\mathrm{ref}})$）/ IPO / PPO / ORPO。邻居只链不改。
- 表数字带分母与模型（GPT-J / Pythia-2.8B / GPT-2-large；Table 2 的 $N$）。长度刷分原文无表，标了「见 04」，未编。
- 质检员建议对读：**§3 反解与 $Z$ 抵消**（对照图 2）和 **§7 Table 2 / TL;DR 61% vs 57%**。
- 未改邻居与 live 三份。
