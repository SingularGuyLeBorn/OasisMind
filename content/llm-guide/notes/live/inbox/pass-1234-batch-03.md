---
title: 1234 过文 · batch 03（2.2.1 → 2.3.1 节首页）
date: 2026-08-30
published: false
---

# batch 03 · 监工点评

硬规则同 batch 01：成文、浅色新图、旧深色文件不删、禁止 Delete / commit / 改 live、禁止抄飞书。飞书对照：`1.3 Attention`、`5.2 FlashAttention`、`5.3 PageAttention`（只对照覆盖面）。

论文截图（Vaswani / GQA / DeepSeek-V2 白底 jpg）**已是浅色则保留**，不要整套重画。InfraTech 超大 jpg 打不开或深色才换浅色 `fig-*.png`。

禁止改 `2.2-基础注意力机制.md`（b02）与 `2.3.5-多头潜在注意力MLA.md`（下一波）。

## 1. `2.2.1-自注意力机制/2.2.1-自注意力机制.md`（~521 行，3 图）

- 优点：QKV 直觉 + 矩阵式；三张自绘图 **已是浅色**（`fig-self-attn-qkv-flow.png` / heatmap / causal-mask），**保留**。
- 缺点：第 14 行写成 `\*\*Query ...\*\*`，Markdown 粗体坏了。
- 改：修转义；成文（无修订块则不动结构）；不要重画三张浅色图。可补一张「点积 → softmax → 加权 V」小图，非必须。

## 2. `2.2.2-多头注意力变体/2.2.2-多头注意力变体.md`（78 行，1 图）

- 优点：节地图该长这样。族图 jpg 浅色，保留。
- 缺点：阅读表没有 **Kimi AttnRes**；2.3.5 还自称「详尽推导」，这里要写清 **公式本体在 01–05，2.3.5 只该当入口**。
- 改：表加一行链 `Kimi-Attention-Residuals-深度维注意力聚合.md`；文首加「不是 2.3 的算子优化」。不要把 MHA 公式抄第二遍。

## 3. `2.2.2/.../01-MHA-多头注意力的标准形式.md`（~431 行，3 图）

- 优点：金样本；点积注意力 jpg 浅色，保留。
- 改：打开另两张 jpg，深则换浅色 `fig-mha-*.png`；不要再推一遍 2.2.1 的单头推导，只钉多头 + KV 参照系。成文。

## 4. `2.2.2/.../02-MQA-共享KeyValue的极致压缩.md`（~373 行，3 图）

- 优点：带宽动机清楚；族图浅色。
- 改：核 3 图；深则换。成文。不要发明 PaLM 没写过的数字。

## 5. `2.2.2/.../03-GQA-在性能与缓存之间折中.md`（~283 行，3 图）

- 优点：$G=H$/$G=1$ 旋钮清楚。
- 改：核图；散点图是论文 Figure 3，浅色则留。链 2.3.1 的「源码对照」那篇，声明 **公式在本篇，那边不重推**。

## 6. `2.2.2/.../04-MLA-低秩潜变量与解耦式注意力.md`（~426 行，3 图）

- 优点：cache 存 $c^{KV}$+$k^R$ 说清楚了。
- 改：核图；链 05 吸收、链 2.3.5「那边应收成导航不要第二套公式」（只改本篇互链，不改 2.3.5 文件）。数字回 V2 论文。

## 7. `2.2.2/.../05-MLA矩阵吸收与非吸收双版本.md`（~496 行，8 图）

- 优点：Prefill 非吸收 / Decode 吸收该在这。
- 缺点：部分 jpg 极大（吸收流图），可能难渲染。
- 改：打不开或深色的换浅色、体积可控的 `fig-mla-absorb-*.png` / `fig-mla-nonabsorb-*.png`；能显示的浅色论文图保留。不要再推 04 的 latent 定义。FLOPs 曲线禁止手绘假坐标，现有复现图浅色则留。

## 8. `2.2.2/.../Kimi-Attention-Residuals-深度维注意力聚合.md`（~195 行，**0 图**，有修订）

- 优点：放在注意力变体而不是 2.1.3，理由对。
- 缺点：无图；K3 用法是修订贴条。
- 改：折修订进正文；**至少一张**浅色「层输出 → 伪查询 → 块摘要」流图；链 2.1.3「不是换残差加法」；链 14.5 K3 D2 只说捆法。不要 git mv 进 `06-` 夹。

## 9. `2.3-高效与稀疏注意力/2.3-高效与稀疏注意力.md`（~483 行，1 图）

- 优点：总图 `fig-ch23-efficient-attn-roadmap.png` **已浅色，保留**。
- 缺点：章首页写成第二份专文；演进表写了两遍；MHA 链接坏了：`../2-核心原理与架构/2.2-...`（文件已在第 2 章夹内，应是 `../2.2-基础注意力机制/...`）。标题残留 `3-高效`。§1 把 2.3.5 写成「详尽推导」。
- 改：收成地图（删重复表、删大段工业数字复述，数字回专文）；修链接；2.3.5 改写成「入口，公式在 2.2.2/04–05」；不要链已删的 10/11/12 章。roadmap/进度.md 不要改。

## 10. `2.3.1-硬件高效注意力/2.3.1-硬件高效注意力.md`（~265 行，1 图，有修订）

- 优点：MEA → FA → Paged 时间线对；总图 `fig-ch231-hw-efficient-attn.png` **已浅色，保留**。
- 缺点：Online Softmax 段后面贴着 `2026-08 修订（不删上文）`。
- 改：把 Milakov 2018 / Rabe 2021 / FA 三家折进 §5 主叙述，删修订块。`03-GQA与MQA` 标成源码对照，**公式本体在 2.2.2**。不要重画总图。
