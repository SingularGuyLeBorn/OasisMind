---
title: 1234 过文 · batch 08（MoE 08–10 + 2.4.2–2.4.7 + JEPA）
date: 2026-08-30
published: false
---

# batch 08 · 监工点评

硬规则同 batch 01。禁止改 2.4 节首页、2.4.1 首页与 01–07（b07）。禁止 live / commit / Delete。飞书对照：`4.8 MOE`（覆盖面）。第 5/14 同一章两面，型号捆法链 14 不抄报告。

## 1. `08-MoE系统优化综述.md`（~214 行）

- 改：核图；成文；不要第二份 DeepSeek-MoE 公式。缺浅色图就补一张「EP / 通信」示意。

## 2. `09-MoE模型量化技术综述.md`（~168 行）

- 改：核图；成文；专家量化数字回论文，不编。

## 3. `10-Stable-LatentMoE与Quantile-Balancing.md`（~83 行，已有 `fig-quantile-balancing.png`）

- 改：核该图深浅；$\ell$ **不是** MLA 的 $c^{KV}$；链 01-SiTU-GLU、14.5 K3。不要编 Quantile 公式。可略补浅色「满宽专家 vs $\ell=d/2$」若现图不够。

## 4. `2.4.2-状态空间模型SSM.md`（~492 行，**0 图**）

- 改：**至少一张**浅色「连续 SSM → 离散 → 卷积核」；成文。不要抄 Mamba 专文后半。

## 5. `2.4.3-Mamba系列.md`（~339 行，**0 图**）

- 改：**至少一张**浅色选择机制 / 2.4.2 链「公式在 SSM，这里讲选型」。成文。

## 6. `2.4.4-线性RNN与Griffin.md`（~387 行，有修订）

- 改：折修订——「$O(1)$」= 不随已生成长度涨；Table 1 $O(Td)$ / $O(d)$；漏 $W_o$；这篇是 RWKV-4。链 2.3.3 / 2.3.4。补浅色 RWKV 状态图若无图。

## 7. `2.4.5-新兴架构与混合模型.md`（~587 行，**0 图**）

- 改：节地图化，长名单改成链；**至少一张**浅色「混合：线性层 + 稀疏/全注意力」；MiniMax-M2 退回 softmax 若提到则链 2.3.3。不要发明架构。

## 8. `2.4.6-多Token预测MTP深度解析.md`（~259 行）

- 改：把 §9 修订折进正文：**V4 MTP 与 V3 相同、不是 EAGLE-3**；K3 才把 MTP 微调成 EAGLE-3 draft + $\mathcal{L}_{LK}$。删「§9 修订」标题。补浅色「一次预测 D 个 token」图。链 6.6.2 / 14.5，不编加速比。

## 9. `2.4.7-扩散语言模型DLM指针.md`（~83 行）

- 改：保持指针，不要抄 `content/diffusion-llm/`。可一张浅色「掩码扩散 vs 自回归」；成文。

## 10. `01-LLM-JEPA：....md`（~331 行）

- 改：核图；成文；JEPA 是联合嵌入预测，**不是** MTP。缺图补浅色「预测表示不是预测 token」。
