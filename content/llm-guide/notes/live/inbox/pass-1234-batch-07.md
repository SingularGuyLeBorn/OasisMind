---
title: 1234 过文 · batch 07（2.3.5 收成 + 2.4 首页 + MoE 01–07）
date: 2026-08-30
published: false
---

# batch 07 · 监工点评

硬规则同 batch 01。禁止改 2.3.2 / 2.3.3 / 2.3.4、禁止改 `2.4.1` 的 08–10 与 2.4.2+（下一波）。禁止改第 5 章夹里的 `*_images` 文件（2.3.5 只改引用）。禁止 live / commit / Delete。

## 1. `2.3.5-多头潜在注意力MLA/2.3.5-多头潜在注意力MLA.md`（~2345 行）

- 优点：本该当 2.3 入口。
- 缺点：第二套完整 MLA 推导；文末从第 5 章拷了 14 张 `image_0.jpg`–`image_13.jpg`；`mla_archive_analogy.png` **深色，必须换**；`fig-mla-latent-cache.png` 里 $C_{KV}$ 是深蓝实心底，按浅色规范重画引用（旧文件留着）。
- 改：**收成导航**，不是再写一篇专文。文首职责：公式与吸收双版本在 [2.2.2/04](…) / [05](…)；本页只留「2.3 为什么还挂 MLA」（KV 表示 vs 算子优化）+ 一张浅色 cache 图 + 「不是」MQA。中间重复公式整段删，改成链。文末 14 张第 5 章图 **改成一条链**（第 5/14 是同一章两面，精读走 14.1 DeepSeek），不要再 embed。不要 git mv 本文件。

## 2. `2.4-前沿架构与变体.md`（43 行）

- 改：把「2026-08 修订」折进 §1：本章不是几乎等于 MoE；索引已有 SSM/Mamba/Griffin/MTP/DLM。删修订节。浅色总图可选一张「MoE / SSM / 线性 RNN / 扩散」四格。

## 3. `2.4.1-混合专家模型MoE.md`（~607 行）

- 优点：多张 `fig-moe-*.png` 已浅色（`fig-moe-dense-vs-sparse.png` 已核）。
- 改：文首修订折进：LatentMoE $\ell=d/2$ **不是** MLA 的 $c^{KV}$；链 10-Quantile-Balancing；MiniMax-M2 sigmoid 8/256 链 14.8。打开其余 fig，深则换。标题别再叫「终极指南」。

## 4. `01-DeepSeek-MoE.md`（~292 行）

- 缺点：`image_0.png` 等像截图；还夹了 MLA 缓存图（不该在 MoE 专文推 MLA）。
- 改：深色截图换浅色 `fig-deepseek-moe-*.png`（共享专家 / 细粒度路由即可，不必一张不漏重画所有 image_N）；MLA 图改成链 2.2.2/04。成文。

## 5. `02-MoE的工程实践.md`（~244 行，16 张 `image_N`）

- 改：先打开 `image_0`–`image_2`。若是语雀/深色截图：**不要 16 张全重画**；补 2–3 张浅色（负载、EP 通信、容量溢出），正文改引用，旧文件留着。成文。

## 6–10. `03` Top-K 可导（薄）· `04` 硬件架构综述 · `05` 系统架构综述 · `06` 硬件加速 · `07` 混合并行图解

- 改：核图深浅；成文；综述页不要第二份 DeepSeek-MoE 公式。03 太薄就补浅色「straight-through / 不可导 Top-K」一张，数字不编。07 若已有图则核主题。
