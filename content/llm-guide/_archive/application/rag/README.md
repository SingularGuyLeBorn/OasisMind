# RAG 历史图像归档

本目录保存旧 `7.2-RAG/images/` 的五张示意图。它们在 2026-09-01 重构后已无公共正文引用，且存在下列问题，因而不能继续作为 RAG 正本证据：

- 把固定 `top-k`、单一向量通道或特定框架类名画成通用架构；
- 缺少权限、版本、稀疏/混合检索、引用、拒答和安全边界；
- 个别图片文字损坏或时间线未完成；
- 没有可追溯的一手数据或版本口径。

归档只用于历史审计，不属于公共课程导航，也不维护其内部语义。后续公共配图应依据新的四条责任链重新生成，并在正文中写明来源、边界和核验日期。
