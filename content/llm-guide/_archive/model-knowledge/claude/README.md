# Claude 旧知识归档说明

> 本目录仅用于迁移追溯，不是公共知识库内容，也不代表其中说法仍然有效。

这里保存 61 个旧文件，共 31,934,866 字节：47 篇旧 Markdown、4 个失效或陈旧 HTML、7 个占位 HTML、2 个可被 PDF 工具检测出交叉引用/流错误的重复 PDF，以及 1 张旧生成示意图。

归档的主要问题包括：

- 在没有一手披露时给 Claude 填写参数量、层数、Dense/MoE、GQA、RoPE、FlashAttention、训练语料与 token 数；
- 为 Opus 4.7 编造“动态混合稀疏注意力”“Meta-Memory”“双通路视觉”等具体架构；
- 将客户陈述、官方自测或不同脚手架的结果写成通用能力事实；
- 将后代模型数字回填到早期版本，或混淆同名模型的不同快照；
- 把 Constitutional AI 论文/宪法文本的公开误写成模型权重开放；
- 用文末追加“纠错”而不改正文，形成互相冲突的双重叙事。

所有旧文件的原路径、唯一新路径、字节数和 SHA-256 见 `_sources/model-reports/claude/migration-ledger.tsv`。公开事实请使用 `05-模型家族与选型/5.3-模型家族/claude/` 下的新页面。
