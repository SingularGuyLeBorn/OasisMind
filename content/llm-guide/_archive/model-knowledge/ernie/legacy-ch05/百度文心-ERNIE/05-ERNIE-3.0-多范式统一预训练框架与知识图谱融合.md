---
title: "05 · ERNIE 3.0 多范式统一预训练框架与知识图谱融合"
---

# ERNIE 3.0：多范式统一预训练框架与知识图谱融合

## 一、模型概览与历史定位

2021 年 7 月，百度发布 ERNIE 3.0，这是全球首个将**自回归生成网络**与**自编码理解网络**统一到一个预训练框架中的大模型，也是首次在千亿级参数规模下系统性地引入大规模知识图谱进行联合训练。ERNIE 3.0 的论文(arXiv:2107.02137)被学术界广泛引用，其提出的"多范式统一"思想影响了后续包括 GPT-4、Gemini 在内的众多大模型设计。

2021 年 12 月，百度联合鹏城实验室推出 **ERNIE 3.0 Titan**，参数规模跃升至 **2600 亿**，成为当时全球最大中文单体模型。该模型在鹏城云脑Ⅱ(搭载数千颗国产 AI 芯片)上完成训练，证明了国产算力基础设施支撑超大规模模型训练的可行性。

| 版本 | 发布时间 | 参数量 | 训练框架 | 关键特性 |
|------|----------|--------|----------|----------|
| ERNIE 3.0 | 2021.07 | 100亿 | 飞桨 + 384×V100 | 多范式统一预训练，知识图谱融合 |
| ERNIE 3.0 Titan | 2021.12 | 2600亿 | 飞桨 + 鹏城云脑Ⅱ | 在线蒸馏，可控/可信学习 |
| ERNIE 3.0 Zeus | 2022.05 | 千亿级 | 飞桨 | 层次化 Prompt 学习，零样本增强 |

ERNIE 3.0 的诞生背景值得回顾：2021 年，GPT-3 已经展示了超大规模自回归模型的惊人能力，但其局限也十分明显——**擅长生成，弱于理解**。BERT 及其变体在理解任务上表现优异，但生成能力有限。业界迫切需要一种能够同时驾驭理解与生成任务的统一架构。ERNIE 3.0 正是对这一需求的回应。

## 二、核心架构：多范式统一预训练框架

### 2.1 自回归 vs 自编码：范式分裂的根源

在 ERNIE 3.0 之前，NLP 领域存在两条泾渭分明的技术路线：

**自编码(Auto-Encoding)路线**：以 BERT 为代表，通过掩码语言建模(MLM)学习双向上下文表征。其优势在于理解任务(分类、抽取、推理)，但生成任务需要额外的解码器模块，且预训练与微调之间存在目标函数不匹配问题。

**自回归(Auto-Regressive)路线**：以 GPT 系列为代表，通过因果语言建模(CLM)学习从左到右的生成能力。其优势在于流畅的文本生成，但双向信息获取受限，理解任务表现弱于同规模 BERT。

两条路线的本质差异在于**信息流动方向**：自编码允许 token 同时关注左右两侧上下文(双向注意力)，自回归只允许关注左侧上下文(因果掩码)。这种架构层面的分歧导致：一个模型无法同时在 GLUE(理解)和摘要/翻译(生成)基准上取得最优。

### 2.2 ERNIE 3.0 的统一框架设计

ERNIE 3.0 的核心创新是将两种范式融合到一个共享 backbone 中，通过**任务特定的表示模块(Task-Specific Representation Module)**在统一语义空间上分叉：

```
输入文本
    ↓
[共享通用表示模块] —— 48层，4096 hidden，64 heads(自编码风格双向注意力)
    ↓
    ├─→ [自然语言理解头] —— 12层，768 hidden，12 heads(自编码，MLM目标)
    │       ↓
    │   情感分析 / NER / MRC / 语义相似度...
    │
    └─→ [自然语言生成头] —— Transformer-XL 结构(自回归，CLM目标)
            ↓
        文本摘要 / 问题生成 / 机器翻译 / 对话...
```

**共享通用表示模块(Universal Representation Module)**采用标准的 Transformer Encoder 结构，使用双向注意力机制，负责提取语义级的通用表征。这一模块是整个模型的"底座"，参数量占比约 85%(100亿模型中约 85 亿参数)。

**任务特定模块**则是轻量级的"适配器"：
- **理解头**：继承 ERNIE 2.0 的多任务学习框架，在 MLM 基础上增加知识掩码(Knowledge Masking)、实体关系预测等任务; 
- **生成头**：基于 Transformer-XL，引入片段级递归机制(Segment-Level Recurrence)，支持超长文本(> 512 tokens)的建模。

### 2.3 统一预训练目标函数

ERNIE 3.0 的预训练损失是三个目标的加权组合：

$$
\mathcal{L} = \lambda_1 \cdot \underbrace{\mathcal{L}_{\text{MLM}}}_{\text{掩码语言建模}} + \lambda_2 \cdot \underbrace{\mathcal{L}_{\text{CLM}}}_{\text{因果语言建模}} + \lambda_3 \cdot \underbrace{\mathcal{L}_{\text{KG}}}_{\text{知识图谱对齐}}
$$

其中：
- $\mathcal{L}_{\text{MLM}}$ 作用于理解头，掩码策略为实体级掩码(Entity-Level Masking)—— 掩码整个实体(如"李白"而非单个字"李"+"白")，迫使模型基于知识而非字符统计进行预测; 
- $\mathcal{L}_{\text{CLM}}$ 作用于生成头，标准的自回归 next-token prediction; 
- $\mathcal{L}_{\text{KG}}$ 为知识图谱对齐损失，下文详述。

训练过程中，三个损失交替优化：每个 batch 中，约 50% 的样本用于 MLM，30% 用于 CLM，20% 用于 KG 对齐。这种交替策略避免了不同目标之间的梯度冲突。

## 三、知识增强：从数据到架构的深度融合

### 3.1 知识图谱数据的预处理与注入

ERNIE 3.0 使用的知识图谱来源于百度百科、百度知心等平台，包含超过 **5500 万实体**和数亿条关系边。与后续 ERNIE 4.5 的"知识适配器"不同，ERNIE 3.0 采用更直接的**知识-文本平行预训练**策略：

**步骤一：知识图谱三元组文本化**

将三元组 $(h, r, t)$(头实体、关系、尾实体)转换为自然语言描述。例如：
- 三元组：(李白, 职业, 诗人)
- 文本化："李白的职业是诗人"

这种文本化使得知识图谱数据可以直接进入标准的语言模型预训练流程，无需设计复杂的图神经网络模块。

**步骤二：知识掩码采样**

在预训练时，ERNIE 3.0 以 20% 的概率将普通文本替换为知识图谱文本化的句子。对于知识句子，采用**关系级掩码(Relation-Level Masking)**：掩码整个关系或尾实体，要求模型基于头实体和关系类型推断缺失信息。例如：

> 输入："李白的 [MASK] 是诗人"
> 目标：模型需要识别出 [MASK] 对应的是"职业"这一关系类型

这种设计迫使模型学习**关系语义**而非单纯的词汇共现。

**步骤三：知识-文本对齐损失**

ERNIE 3.0 引入了基于对比学习的知识对齐损失：

$$
\mathcal{L}_{\text{KG}} = -\log \frac{\exp(\text{sim}(h_{\text{text}}, h_{\text{kg}}) / \tau)}{\sum_{h' \in \mathcal{N}} \exp(\text{sim}(h_{\text{text}}, h') / \tau)}
$$

其中 $h_{\text{text}}$ 是文本中实体的隐藏状态，$h_{\text{kg}}$ 是知识图谱中对应实体的嵌入，$\mathcal{N}$ 是负采样集合，$\tau$ 为温度系数。这一损失函数确保模型在语义空间中将文本实体与知识图谱实体对齐。

### 3.2 可控与可信学习算法

ERNIE 3.0 Titan 在基础版本之上增加了**可控学习(Controllable Learning)**和**可信学习(Trustworthy Learning)**两大机制：

**可控学习**：通过引入控制码(Control Codes)指导生成风格。控制码是一组可学习的嵌入向量，在生成头的输入层与 token embedding 相加。不同的控制码对应不同的生成风格(如：新闻体、口语化、学术体、诗歌体)。训练时，模型学习将控制码映射到特定的生成分布：

$$
P(x | c) = \prod_{t=1}^{T} P(x_t | x_{<t}, c)
$$

其中 $c$ 是控制码嵌入。ERNIE 3.0 Titan 预定义了 16 种控制码，覆盖文体、情感极性、长度约束等维度。

**可信学习**：针对大模型的事实幻觉问题，ERNIE 3.0 Titan 在训练阶段引入**事实一致性判别器(Factuality Discriminator)**。判别器是一个二分类网络，输入为生成文本和对应的知识图谱子图，输出为事实一致性分数。生成头的训练目标修改为：

$$
\mathcal{L}_{\text{gen}}' = \mathcal{L}_{\text{CLM}} - \lambda \cdot \log D(x_{\text{gen}}, G_{\text{kg}})
$$

其中 $D$ 是判别器，$G_{\text{kg}}$ 是相关知识子图。通过最大化判别器的输出(即让生成文本被判别器判定为"事实一致")，模型在生成过程中自动倾向于知识图谱支持的内容。

## 四、ERNIE 3.0 Titan：2600 亿参数的国产超算实践

### 4.1 模型规格与训练配置

ERNIE 3.0 Titan 的架构在 ERNIE 3.0 基础上进行了深度扩展：

| 组件 | ERNIE 3.0 | ERNIE 3.0 Titan |
|------|-----------|-----------------|
| 通用表示层数 | 48 | 64 |
| 隐藏维度 | 4096 | 8192 |
| 注意力头数 | 64 | 128 |
| 总参数量 | 100 亿 | 2600 亿 |
| 训练数据量 | 4TB | 10TB+ |
| 训练硬件 | 384×V100 | 鹏城云脑Ⅱ(数千颗国产芯片) |
| 训练框架 | 飞桨 | 飞桨自适应分布式 |

### 4.2 飞桨自适应分布式训练技术

ERNIE 3.0 Titan 的训练依托百度飞桨的**端到端自适应分布式架构**，这一架构在业界具有开创性意义：

**资源感知分配(Resource-Aware Allocation)**

传统分布式训练通常采用固定的并行策略(如数据并行 + 张量并行 8 路)，但不同训练阶段的计算特征不同——前向传播是计算密集型，反向传播的梯度同步是通信密集型。飞桨的资源感知分配器动态调整并行策略：

$$
\text{Strategy}(t) = \arg\max_{s \in \mathcal{S}} \frac{\text{Throughput}(s, t)}{\text{Communication\_Cost}(s, t) + \text{Memory\_Cost}(s, t)}
$$

其中 $\mathcal{S}$ 是候选策略集合(数据并行、张量并行、流水线并行及其组合)，$t$ 是训练步数。实验表明，自适应分配使 ERNIE 3.0 Titan 的训练吞吐量提升了 **2.1 倍**。

**参数分片与重计算**

2600 亿参数远超单卡显存容量(即使是 80GB A100)。飞桨采用**参数分片(Parameter Sharding)**技术：将模型参数划分为多个分片，每个计算节点只存储一部分参数，需要时通过 all-gather 通信获取。结合**激活值重计算(Activation Recomputation)**—— 不保存中间激活值，反向传播时重新计算—— 将显存占用降低了 60%。

**混合精度与通信压缩**

训练使用 FP16/BF16 混合精度，并引入**1-bit Adam 优化器**：将梯度通信压缩至 1-bit(仅传输符号位)，在几乎不损失收敛性的前提下将通信带宽需求降低 32 倍。这对于基于国产芯片的鹏城云脑Ⅱ集群尤为重要，因为国产芯片间的互联带宽通常低于英伟达 NVLink。

### 4.3 在线蒸馏：大模型落地的成本破局

ERNIE 3.0 Titan 的另一项工程创新是**大模型在线蒸馏框架**。传统知识蒸馏需要分别训练教师模型和学生模型，流程繁琐且教师模型在蒸馏阶段处于"冻结"状态，无法继续优化。ERNIE 3.0 的在线蒸馏允许教师模型和学生模型**同时训练、相互促进**：

```
教师模型(2600亿参数) ←—— 监督信号 ——→ 学生模型(10亿/100亿参数)
       ↑                                        ↑
       └──────── 蒸馏损失共享梯度 ───────────────┘
```

具体实现上，每个训练 step 包含两个阶段：
1. **教师前向**：用当前 batch 计算教师模型的输出分布 $P_T$; 
2. **联合反向**：学生模型以 KL 散度最小化 $P_S$ 与 $P_T$ 的距离，同时教师模型也以学生模型的困难样本为负例进行强化学习。

在线蒸馏使得 ERNIE 3.0 Titan 在训练过程中就能产出多个尺寸的学生模型，企业无需额外蒸馏即可直接部署 10 亿或 100 亿参数的"轻量版"，落地成本降低 **80%**。

## 五、性能评估与横向对比

### 5.1 中文 NLP 基准测试

ERNIE 3.0 在 54 个中文 NLP 任务上进行了全面评估，涵盖 14 种任务类型：

| 任务类型 | 数据集数量 | ERNIE 3.0 | GPT-3 (175B) | BERT-wwm-ext |
|----------|------------|-----------|--------------|--------------|
| 情感分析 | 5 | 95.8 | 89.2 | 93.1 |
| 命名实体识别 | 4 | 93.5 | 85.7 | 91.2 |
| 机器阅读理解 | 6 | 89.2 | 82.4 | 85.6 |
| 语义相似度 | 4 | 91.4 | 84.1 | 88.3 |
| 文本摘要 | 3 | 86.7 | 81.5 | N/A |
| 问答生成 | 2 | 88.3 | 79.6 | N/A |
| 零样本分类 | 8 | 78.5 | 72.3 | 45.2 |

在理解任务上，ERNIE 3.0(100亿参数)超越了 175B 的 GPT-3，验证了**知识增强 + 双向注意力**路线在理解任务上的效率优势。在生成任务上，ERNIE 3.0 与 GPT-3 的差距缩小到 3-5 个百分点，而参数仅为 GPT-3 的 1/17。

### 5.2 SuperGLUE 与跨语言基准

在 SuperGLUE(国际权威语言理解评测)上，ERNIE 3.0 Titan 以超越人类水平 **0.8 个百分点**的成绩登顶全球榜首(截至 2022 年初)。在 XTREME 跨语言基准上，ERNIE-M(基于 ERNIE 3.0 扩展的多语言版本)在 96 种语言的 9 项任务中取得 SOTA。

### 5.3 零样本与小样本能力

ERNIE 3.0 在零样本(Zero-Shot)和小样本(Few-Shot)场景下表现尤为突出。在 18 个零样本数据集上，ERNIE 3.0 刷新了当时的 SOTA：

| 数据集 | 任务 | ERNIE 3.0 Zero-Shot | GPT-3 Zero-Shot |
|--------|------|---------------------|-----------------|
| CMNLI | 自然语言推理 | 71.2 | 64.8 |
| C3 | 阅读理解 | 68.5 | 61.3 |
| LCQMC | 语义相似度 | 82.1 | 75.6 |
| DRCD | 文档级阅读 | 74.3 | 67.9 |

零样本能力的优势源于知识图谱的注入：对于"李白的职业是什么"这类问题，即使没有见过相似的训练样本，模型也能通过内部化的知识图谱直接回答。

## 六、ERNIE 3.0 的工程影响与产业落地

### 6.1 对后续大模型设计的影响

ERNIE 3.0 的"多范式统一"思想在 2021 年颇具前瞻性，其影响体现在：

- **GPT-4(2023)**：OpenAI 未公开 GPT-4 的架构细节，但业界普遍认为 GPT-4 采用了类似的"统一 backbone + 多任务头"设计，以同时支持对话、代码、多模态等任务; 
- **T5 / UL2(Google)**：2022 年发布的 UL2 框架明确借鉴了 ERNIE 3.0 的多范式思想，提出 Mixture-of-Denoisers(MoD)统一了多种预训练目标; 
- **文心一言(2023)**：ERNIE 3.0 是文心一言(ERNIE Bot)的技术底座，其对话能力直接继承自 ERNIE 3.0 的生成头。

### 6.2 产业落地案例

截至 2024 年，ERNIE 3.0 及其衍生模型已在百度百余个产品中部署：

- **百度搜索**：问题意图分类、网页相关性排序、答案摘要生成; 
- **百度 Feed 流**：新闻内容推荐、去重、标题生成; 
- **小度智能屏**：多轮对话意图理解、知识问答; 
- **百度地图**：POI 检索意图解析、导航指令生成; 
- **千帆平台**：为企业提供模型微调、API 调用服务，覆盖金融、医疗、政务等垂直领域。

ERNIE 3.0 Titan 的在线蒸馏技术使得这些应用场景能够以可控的成本部署大模型能力。例如，百度搜索的在线问答模块使用的是 100 亿参数的学生模型，响应延迟 < 100ms，而背后提供知识监督的则是 2600 亿的教师模型。

## 七、局限与后续演进

### 7.1 架构层面的局限

ERNIE 3.0 的多范式统一框架在理论上优雅，但工程实现存在挑战：

1. **任务头之间的干扰**：理解头和生成头共享通用表示模块，但两者的最优表征空间并不完全一致。实验表明，当生成任务比例过高时，理解任务性能会下降 2-3%; 
2. **知识图谱覆盖不足**：ERNIE 3.0 使用的知识图谱以百科类事实知识为主，对动态知识(如实时新闻、股价)和领域专业知识(如法律条文、医学指南)覆盖有限; 
3. **上下文长度限制**：基于 Transformer-XL 的生成头理论上支持长文本，但实际训练时序列长度限制在 512-1024 tokens，无法处理整篇文档级输入。

### 7.2 向 ERNIE 4.x 的演进

ERNIE 3.0 的局限在后续版本中得到了针对性解决：

- **ERNIE 4.0(2023)**：引入 MoE 架构和万亿级激活参数，通过稀疏化缓解任务头干扰; 增加插件机制，用外部工具补充知识图谱的动态知识缺口; 
- **ERNIE 4.5(2025)**：彻底转向异构多模态 MoE，将统一框架从文本扩展到视觉-语言; 采用自适应分辨率视觉编码器，突破固定序列长度限制。

## 八、总结

ERNIE 3.0 是中文大模型发展史上的里程碑。其**多范式统一预训练框架**首次在架构层面打通了自然语言理解与生成两大任务，证明了"一个 backbone、多个任务头"的可行性; **知识图谱融合**则从数据层面为模型注入结构化知识，显著提升了事实准确性和零样本能力; **在线蒸馏**和**可控/可信学习**等工程创新，则为大模型的产业落地提供了成本可控的路径。

ERNIE 3.0 Titan 在鹏城云脑Ⅱ上的训练实践，也证明了国产算力基础设施支撑超大规模模型训练的能力。在 GPT-3 主导的国际大模型格局中，ERNIE 3.0 以"知识增强"为差异化卖点，走出了一条具有中国特色的技术路线。这一路线在后续的文心一言、ERNIE 4.5 中得到了延续和深化，成为国产大模型参与全球竞争的核心技术资产。

---

>  **延伸阅读**
> - [ERNIE 3.0 论文：arXiv:2107.02137](https://arxiv.org/abs/2107.02137)
> - [ERNIE 3.0 Titan 技术博客](http://research.baidu.com/Blog/index-view?id=165)
> - [飞桨大模型分布式训练文档](https://www.paddlepaddle.org.cn/docs)
> - [鹏城云脑Ⅱ算力平台](https://www.pcl.ac.cn/html/946/)


---

>  **双向同步**
> - 本文件为 `14-主流开源模型全景解析与技术报告精读/14.21-Erine/02-ERNIE-3.0/05-ERNIE-3.0-多范式统一预训练框架与知识图谱融合.md` 的知识库同步版本
> - 主文档更新时，本文件将同步更新
