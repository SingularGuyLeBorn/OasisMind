---
title: "2.1.3 · 残差连接"
as_of: 2026-08-30
---
# 2.1.3 残差连接：从 ResNet 到 Transformer 的稳定主干

残差连接(Residual Connection)是现代深度学习里最不起眼、却最不能缺的结构之一. 它看起来只是一个简单的相加操作，却几乎重写了深层网络的训练方式. 没有它，网络一旦变深，梯度传播、信息保持和优化稳定性都会迅速恶化; 有了它，模型才真正获得了「安全地变深」的能力.

很多人第一次看到残差连接时，会觉得它只是一个工程技巧：把输入 `x` 直接加回输出就完了. 但如果只把它理解成技巧，就会低估它的地位. 残差连接真正解决的，不是某一个孤立问题，而是深层网络里的一个系统性矛盾：**模型既要不断加工表示，又不能在每一层都把已有信息和梯度通路彻底重写. **

对大语言模型来说，这种矛盾比 ResNet 时代更尖锐. 因为 Transformer 不只是深，而且每一层都同时包含 Attention、FFN、归一化和残差主干. 模型要想在几十层、上百层甚至更深的结构里保持可训练、可扩展、可部署，残差连接几乎是默认前提.

标准残差是单条流 $H(x)=F(x)+x$。2024 年起 **Hyper-Connections (HC)** 把残差扩成 $N$ 条可学习混合的流；**mHC** 用双随机约束把深度连乘稳住，主设定 **$N=4$**；**xHC** 站在 mHC 上把记忆宽度扩到 **$N=16$**，每子层只更新 **$k=4$**（不要把 $k=4$ 写成 mHC 的 $N$）；**Gated Residual** 同样加宽到 $n_r=4$，但丢掉 $H_{\mathrm{res}}$、读用逐元素门。专文：[01-Hyper-Connections 与 mHC](./01-Hyper-Connections与mHC/01-Hyper-Connections与mHC.md) · [02-xHC：Expanded Hyper-Connections](./02-xHC-Expanded-Hyper-Connections/02-xHC-Expanded-Hyper-Connections.md) · [03-Gated Residual](./03-Gated-Residual/03-Gated-Residual.md)。**不要和 HCA 混名，也不要把 GR 写成 [Gated Attention $G_1$](../../2.2-基础注意力机制/2.2.2-多头注意力变体/06-Gated-Attention-SDPA输出门控/06-Gated-Attention-SDPA输出门控.md)。** AttnRes 是深度维注意力聚合，见 [2.2.2 AttnRes](../../2.2-基础注意力机制/2.2.2-多头注意力变体/08-AttnRes-深度维注意力聚合/08-AttnRes-深度维注意力聚合.md)。

本文围绕五个问题展开：

1. 残差连接到底在解决什么问题.
2. 为什么「学习修正」比「重学映射」更容易.
3. 梯度高速公路到底是怎么形成的.
4. 为什么残差连接在 Transformer 里会与 Pre-Norm、FFN、Attention 深度绑定.
5. 为什么残差连接会继续成为大模型工程的基础设施.

## 1. 残差连接到底在解决什么问题

在残差连接出现之前，深度学习里有一个很反直觉的现象：网络更深，理论上表达能力更强，但实际训练结果反而可能更差. 这种现象通常被称为**退化问题**. 这里的「退化」不是过拟合，而是说：即使在训练集上，更深的网络也不一定比更浅的网络优化得更好.

这背后有两个核心原因. 第一，梯度路径变得过长. 网络每多一层，反向传播就要再多穿过一次局部 Jacobian; 层数一多，梯度就更容易在连乘中衰减或爆炸. 第二，前向信息会被层层覆盖. 每一层都在改写表示，如果没有保底通路，早期层里本来有价值的信息很容易在中途丢失.

换句话说，深层网络并不只是「层更多」，而是同时面临两种风险：

1. **梯度难以安全回传**
2. **输入信息难以稳定保留**

残差连接的价值，就在于它同时给这两件事加了一条保底路径.

## 2. 残差连接的数学形式：为什么是 $H(x)=F(x)+x$

残差连接最经典的写法是：

$$
H(x)=F(x)+x \tag{1}

$$

这里 `x` 是输入表示，`F(x)` 是若干层网络学到的变换结果，`H(x)` 是残差块最终输出. 这个公式表面上只是多了一项输入直连，但它彻底改变了网络的学习目标.

如果没有残差连接，子网络必须直接学会完整映射 `H(x)`. 有了残差连接后，子网络只需要学习：

$$
F(x)=H(x)-x \tag{2}

$$

也就是说，它不再被要求「重做整个映射」，而是只需要学习「在现有表示基础上补多少修正」.

这就是残差的本质：**网络学的不是从零开始的重建，而是在已有结果上的增量修正. ** 在很多实际任务里，这比直接学习完整映射容易得多. 因为如果当前层什么都不做其实已经不算太差，那么最优更新往往更接近一个小修正，而不是一场彻底重写.

![直接映射 H(x) 与残差学习 F(x)+x 对照](./images/fig-residual-direct-vs-skip.png)

> 图 1：残差学习把「直接拟合完整映射」改成了「只学习增量修正」，从而显著降低优化难度。

**图 1 解析**

- 左：整块必须学会 $H(x)$，没有退路。
- 右：旁路学 $F(x)$，蓝线恒等捷径原样加回去，$H(x)=F(x)+x$。
- 若 $F\to 0$，整块退化成恒等；这就是「更深至少不要更差」的结构保证。

## 3. 为什么学习修正比学习重建更容易

如果某一层最理想的行为只是「大体保持输入不变，只做一点局部修正」，那么普通网络必须想办法把很多非线性层联合优化成近似恒等映射. 这在实践中并不容易. 相反，残差结构只要让 `F(x)` 趋近于零，就能自然退化成恒等映射.

这件事很重要，因为它让「更深的网络至少不要比浅层更差」变成了可实现目标. 哪怕新增层暂时没学到特别有用的东西，它也可以先学会不破坏原有表示. 于是深层网络不再被迫“一上来就每层都很有用”，而是允许部分层先当保守修正器，之后再逐步承担更多表达任务.

从优化角度看，这等于把很多难问题变成了更接近零中心的小问题. 一个小修正通常比一个完整替代更容易靠梯度下降找到.

## 4. 梯度高速公路是怎么形成的

残差连接最常见的说法之一，是它构造了「梯度高速公路」. 这句话不是比喻口号，而是可以直接从求导里看出来. 若：

$$
y=x+F(x) \tag{3}

$$

这里的 $L$ 表示模型训练时的损失函数，也就是模型当前输出与目标答案之间的总体误差. 那么损失函数 $L$ 对输入 $x$ 的梯度为：

$$
\frac{\partial L}{\partial x}
=
\frac{\partial L}{\partial y}
\left(I+\frac{\partial F(x)}{\partial x}\right) \tag{4}

$$

这里最关键的不是 $\frac{\partial F(x)}{\partial x}$，而是那个恒等项 $I$. 它意味着：即使子层内部的梯度变得很弱，主干路径上仍然存在一条不依赖子层质量的直接回传通路.

更直白地说，残差连接并不保证梯度永远漂亮，但它保证了梯度**不至于完全被局部坏层堵死**. 这对深层网络训练极其关键. 因为现代大模型的现实并不是“每一层都训练得很完美”，而是“局部不完美依然要让全局训练继续”.

## 5. 残差连接如何保护前向信息

残差连接不只在反向传播里有价值，在前向传播里也同样重要. 没有残差时，模型默认每层都在重写表示; 有了残差后，模型默认保留原输入，再叠加修正. 这使得前面的有用信息不容易被后面的子层彻底覆盖.

这对语言模型尤其关键. 一个 token 的最终表示，往往同时承载：

1. 原始词义
2. 句法角色
3. 上下文依赖
4. 任务指令偏置
5. 领域风格信息

如果每层都完全覆盖上一层表示，这些不同层次的信息很容易彼此冲刷. 残差主干让它们更容易以叠加方式共存，而不是以“后层彻底替换前层”的方式竞争.

![残差主干保留原表示，旁路叠加修正](./images/fig-residual-backbone.png)

> 图 2：残差主干负责保留原表示，子层分支负责叠加修正，因此信息更容易以「保留 + 增量」方式持续流动。

**图 2 解析**

- 蓝线 identity path 把 $x$ 原样送到加法点。
- 橙线经过 $W_1,\ldots,W_L$ 产出 $F(x)$。
- 若 $F(x)=0$，则 $y=x$：子层学砸了也不拆主干。

## 6. 从 ResNet 到 Transformer：残差连接的角色怎么变了

在 ResNet 中，残差连接最直接的任务是解决卷积网络变深后的优化退化问题. 但到了 Transformer 里，它的角色进一步升级了.

Transformer 每层通常都包含两个重型子层：

1. Attention
2. FFN

这两个子层都可能对输入做很强变换. 如果没有残差主干，表示在层间会漂移得非常厉害，梯度路径也会更脆弱. 因此在 Transformer 里，残差连接已经不只是“帮助训练”，而是**构成整个主干信息流的默认骨架**.

更具体地说，Attention 和 FFN 更像在主干上不断写入修正项，而不是彻底接管主干. 现代大模型真正持续贯通全网的，往往不是某个子层，而是残差主路径本身.

## 7. 残差连接为什么与 Pre-Norm 深度绑定

残差连接在 Transformer 里之所以能发挥到极致，与归一化层位置密切相关. 最关键的分歧就是：

Post-Norm：

$$
x_{l+1}=\text{Norm}(x_l+F(x_l)) \tag{5}

$$

Pre-Norm：

$$
x_{l+1}=x_l+F(\text{Norm}(x_l)) \tag{6}

$$

两者差别不只是「Norm 放哪儿」，而是**残差主干是否保持纯净恒等通路**. 在 Pre-Norm 里，输入 $x_l$ 几乎原样沿主干往下走，Norm 和子层都被放到旁路修正里; 在 Post-Norm 里，连主干相加后的结果也要立刻再穿过一层归一化.

这也是为什么深层大模型几乎一边倒选择 Pre-Norm. 它优先保护的是那条最重要的主干：不让残差路径本身被每层额外数值变换反复打扰.

## 8. 残差连接的工程意义：不只是稳定训练

残差连接的工程价值远不止「能训练更深」. 在现代大模型里，它至少还影响四件事：

1. **量化稳定性**主干保留了更稳定的参考表示，子层只做增量修正，这通常比完全覆盖式变换更容易控制量化误差.
2. **参数高效微调**LoRA、Adapter 等方法本质上也是在主干上添加增量修正. 残差结构天然适合这类“外挂式改动”.
3. **多阶段训练迁移**预训练、SFT、对齐、蒸馏等多个阶段，都更容易建立在稳定主干之上做增量行为修正，而不是整网重写.
4. **模块可组合性**
   新模块、新桥接器、新工具路径，更容易以旁路修正方式接入，而不是要求完全替代原有主干.

这些现象背后是同一个原则：**稳定主干 + 增量修正** 比 **每一层都彻底重写** 更适合大规模系统演化.

## 9. 训练和部署时应该重点看什么

如果你在训练或部署一个深层模型，残差路径本身也值得单独观察. 最值得看的通常有：

1. 残差分支输出与主干幅值的比例
2. 各层 residual add 前后的 RMS / 方差变化
3. 深层主干幅值是否持续扩张
4. 子层是否长期学不到有效修正
5. 量化后 residual add 是否引入明显精度损失

一个很常见的误区是：以为 residual add 只是 “`x + something`” 的机械操作，不值得看. 但在深层网络里，残差分支太强会破坏主干，太弱又说明子层不起作用. 它不是“有没有”的问题，而是“修正强度是否合理”的问题.

![残差诊断：主干/修正比、RMS 漂移、量化敏感](./images/fig-residual-diagnostics.png)

> 图 3：残差连接的工程检查重点，不只是「有没有主干」，而是「主干与修正分支之间的幅值关系是否健康」。

**图 3 解析**

- 左：identity backbone 与 correction branch 在 residual add 汇合。
- 右：分支/主干比、RMS 漂移、梯度余弦、量化 $\Delta$PPL 是检查项示意，不是某次训练日志。
- $F$ 太强会冲掉主干，太弱说明子层没干活。

## 10. 极简实现：残差块与 Transformer 残差主干

最简形式的残差块，可以写成：

```python
import torch
import torch.nn as nn

class ResidualBlock(nn.Module):
    def __init__(self, module: nn.Module):
        super().__init__()
        self.module = module

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return x + self.module(x)

class PreNormResidual(nn.Module):
    def __init__(self, norm: nn.Module, module: nn.Module):
        super().__init__()
        self.norm = norm
        self.module = module

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return x + self.module(self.norm(x))
```

第一段是最基础的残差结构：主干原样通过，旁路负责增量修正. 第二段则更接近现代大模型：在修正分支前先做归一化，但主干本身尽量保持纯净.

这也说明了残差连接的实现并不复杂，复杂的是它在整个系统中的作用. 它之所以重要，不是因为代码难，而是因为它决定了深层模型的默认信息流到底长什么样.

## 11. 技术前瞻：残差连接还会怎么演化

残差连接本身已经非常经典，但它仍然有演化空间. 未来最值得持续关注的方向包括：

1. 残差幅值控制会更精细例如 residual scaling、DeepNorm、不同层深度下的主干配方优化.
2. 与量化路径的协同会更重要特别是在低比特推理中，residual add 的精度选择可能直接影响模型稳定性.
3. 与模块化插桩的关系会更紧密Adapter、LoRA、工具增强、多模态桥接等路线，本质上都在继续利用残差哲学.
4. 更复杂主干结构可能出现
   例如多分支残差、门控残差、带权重的残差融合。公式与图见 [01 mHC](./01-Hyper-Connections与mHC/01-Hyper-Connections与mHC.md)、[02 xHC](./02-xHC-Expanded-Hyper-Connections/02-xHC-Expanded-Hyper-Connections.md)、[03 Gated Residual](./03-Gated-Residual/03-Gated-Residual.md)。xHC 是残差主干的 scaling 轴，**不要和 HCA 混名**。

但无论形式怎么演化，残差连接的核心原则大概率不会变：**默认保留已有表示，只让子层为真正必要的变化负责. **

## 12. 本节小结

残差连接之所以成为现代深度学习的基础设施，不是因为它数学上多复杂，而是因为它极其现实地解决了深层系统的核心矛盾：模型越深，越需要一条不依赖局部子层质量的稳定主干.

它让层学会做修正，而不是重做; 让梯度学会穿越，而不是挣扎; 让表示学会累积，而不是不断被冲刷. 从 ResNet 到 Transformer，再到今天的大语言模型，残差连接已经不只是一个历史技巧，而是深层模型设计里最稳定、最持久、也最不可替代的原则之一.

## 13. 参考文献

1. [He, K., Zhang, X., Ren, S., &amp; Sun, J. (2016). Deep Residual Learning for Image Recognition.](https://arxiv.org/abs/1512.03385) *CVPR / arXiv*.
2. [Vaswani, A., et al. (2017). Attention Is All You Need.](https://arxiv.org/abs/1706.03762) *NeurIPS / arXiv*.
3. [Ba, J. L., Kiros, J. R., &amp; Hinton, G. E. (2016). Layer Normalization.](https://arxiv.org/abs/1607.06450) *arXiv:1607.06450*.
4. [Zhang, B., &amp; Sennrich, R. (2019). Root Mean Square Layer Normalization.](https://arxiv.org/abs/1910.07467) *NeurIPS Workshop / arXiv*.
5. [Zhu, D., et al. (2024/2025). Hyper-Connections.](https://arxiv.org/abs/2409.19606) *arXiv:2409.19606*.
6. [Xie, Z., et al. (2025/2026). mHC: Manifold-Constrained Hyper-Connections.](https://arxiv.org/abs/2512.24880) *arXiv:2512.24880*.
7. [Zhang, X., et al. (2026). xHC: Expanded Hyper-Connections.](https://arxiv.org/abs/2607.14530) *arXiv:2607.14530*.
