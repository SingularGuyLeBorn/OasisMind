---
title: "04 · MoE 硬件架构研究综述"
date: 2026-05-16
tags: [MoE, 硬件架构, FPGA, 加速器, 稀疏计算, UbiMoE]
---

# MoE 硬件架构研究综述

> 本文梳理 MoE 模型在硬件层面的研究进展, 涵盖从宏观架构设计到微观计算优化的两条技术路线. 

---

## 1. 宏观架构路线：适配 MoE 动态性的系统设计

### 1.1 核心挑战

MoE 的路由动态性导致：
- 不同输入激活不同专家, 计算需求不可预测
- 专家分布在不同 GPU, 通信模式动态变化
- 负载不均衡导致部分 GPU 空闲、部分过载

### 1.2 UbiMoE：FPGA 上的 MoE 加速器

**核心设计**：
- **双核架构**：
  - 延迟优化的流式注意力核
  - 可复用线性核
- **自动搜索算法**：根据 FPGA 资源约束自动调整硬件参数

**优化策略**：
- 注意力核优化数据加载：固定 Q, 广播 K, 降低访存
- 融合 Softmax 核：动态取最大值, 减少内存访问
- 混合计算模式：根据输入动态选择计算策略

---

## 2. 微观优化路线：计算、访存、通信的突破

### 2.1 计算优化

**稀疏矩阵乘法**：
- MoE 的 FFN 是稀疏激活的, 标准 GEMM 效率低
- 需要专门的稀疏矩阵加速器
- 动态调度：根据路由结果实时选择计算单元

**量化计算**：
- 低比特(INT8/FP8)矩阵乘法
- 混合精度： gate 网络用高精度, 专家用低精度

### 2.2 访存优化

**专家权重管理**：
- 专家数量多(256+), 权重总量大
- 按需加载：只加载被激活的专家权重
- 权重共享：多个专家共享部分参数

**KV Cache 压缩**：
- MLA：将 KV Cache 压缩到 1/4
- 分页管理：类似 PagedAttention 的分块管理

### 2.3 通信优化

**All-to-All 优化**：
- 细粒度调度：将 token 按目标专家分组发送
- 通信-计算重叠：在通信时进行其他计算
- 拓扑感知：根据网络拓扑优化路由

---

## 3. 硬件-算法协同设计

### 3.1 算法层面的适配

- **细粒度路由**：减少单个专家的计算量, 增加并行度
- **负载均衡损失**：在训练时鼓励均衡的路由
- **专家共享**：减少专家数量, 降低访存压力

### 3.2 硬件层面的支持

- **稀疏计算单元**：支持动态稀疏模式的矩阵乘法
- **大容量片上内存**：缓存更多专家权重
- **高速互联**：降低 All-to-All 通信延迟

---

## 4. 未来方向

1. **专用 MoE 芯片**：为 MoE 架构设计的 ASIC
2. **光互联**：突破电互联的带宽瓶颈
3. **近存计算**：将计算单元放入 HBM 堆栈中
4. **动态重配置**：根据输入动态调整硬件配置

---

## 5. 总结

MoE 的硬件研究从两条路线并进：

| 路线 | 关注点 | 代表工作 |
|:-----|:------|:--------|
| 宏观架构 | 系统级动态性适配 | UbiMoE (FPGA) |
| 微观优化 | 计算/访存/通信突破 | 稀疏 GEMM、量化、分页管理 |

算法与硬件的协同设计是释放 MoE 潜力的关键. 

> 参考来源：[MoE 研究进展：算法、系统与架构综述(四)-架构篇](https://zhuanlan.zhihu.com/p/1960776777216550865)
