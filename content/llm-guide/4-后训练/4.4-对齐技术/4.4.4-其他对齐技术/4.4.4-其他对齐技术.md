---
title: "4.4.4 · 其他对齐技术"
date: 2026-08-31
as_of: 2026-08-31
tags: [SLiC, RRHF, IPO, Nash-MD, Best-of-N, 对齐技术]
---

# 4.4.4 其他对齐技术

这一夹收不走 PPO、也不走 DPO 分类损失的路。SLiC 用有间隔的 rank hinge 校准序列对数似然；RRHF 用长度归一的 $p_i$ 做无间隔 hinge；IPO 把对数比之差回归到 $\tau^{-1}/2$；PRO 把 $n$ 长序收成 Plackett-Luce；CPO 在翻译上把参考换成均匀先验再加 chosen NLL。Nash-MD 先学成对偏好模型，再对几何混合对手做正则策略梯度，求的是偏好博弈的 Nash，不是标量 Elo。Best-of-$n$ 不是训练损失：解码时采 $n$ 条、代理 RM 取 $\arg\max$，主线是过优化标度。KTO 的正本在 [03-KTO](../4.4.2-无奖励模型的对齐DPO-KTO/03-KTO-前景理论对齐/03-KTO-前景理论对齐.md)，不要在这页再推一遍。公式和表以各专文为准。

| 编号 | 专文 | 钉什么 |
|------|------|--------|
| 01 | [SLiC](./01-SLiC-序列似然校准/01-SLiC-序列似然校准.md) | rank hinge + 对 $y_{\mathrm{ref}}$ 的 CE；间隔原式写 $\beta$，SLiC-HF 改成 $\delta$，实验仍报 $1.0$；sample-rank 对 direct；无 $\pi_{\mathrm{ref}}$ KL |
| 02 | [RRHF](./02-RRHF-排序响应对齐/02-RRHF-排序响应对齐.md) | $p_i$ 是长度归一条件对数概率；$L_{\mathrm{rank}}=\sum_{r_i<r_j}\max(0,p_i-p_j)$，无 margin；再加 $L_{\mathrm{ft}}$ 只打 $\arg\max r$；不是 Bradley-Terry 的 $\sigma$ |
| 03 | [IPO](./03-IPO-身份偏好优化/03-IPO-身份偏好优化.md) | Ψ 取恒等；正则写 $\tau$ 不写 DPO 的 $\beta$；采样损失把 $h_\theta$ 回归到 $\tau^{-1}/2$；实验主体是 3-action 玩具 |
| 04 | [PRO](./04-PRO-偏好排序优化/04-PRO-偏好排序优化.md) | 式 (4) 是 $n-1$ 次 listwise softmax；分数式 (6) 长度归一 $\log P$；$n=2$ 仍不是 DPO；$\beta=0.05(l-1)^{2}$ |
| 05 | [CPO](./05-CPO-对比偏好优化/05-CPO-对比偏好优化.md) | $\pi_{\mathrm{ref}}:=U$ 得到式 (3)，再加 chosen NLL；主战场 MT / ALMA-R；不是 SimPO 的长度平均 |
| 06 | [Nash-MD](./06-Nash-MD-纳什镜像下降/06-Nash-MD-纳什镜像下降.md) | 成对 $\mathcal{P}$ 的 Nash；对手是 $\pi_t$ 与 $\mu$ 的几何混合；$\beta=0$ 才是 Self-Play，不是 SPIN；$\tau$ 不是 IPO 靶心 |
| 07 | [Best-of-N](./07-Best-of-N-奖励模型过优化/07-Best-of-N-奖励模型过优化.md) | 解码 $\arg\max$，不是训练损失；$\mathrm{KL}_{\mathrm{bon}}=\log n-(n-1)/n$；金标 6B / 策略 1.2B；不是 RAFT |

本夹曾经把 RRHF 写成 BT 成对 $\sigma$、把 IPO 靶心写成 $1/(2\beta)$、把 SLiC 写成只有 hinge 没有 CE。以专文为准。

## 1. 这几条专文怎么分

[4.4.1](../4.4.1-基于奖励模型的RL-RLHF-PPO/4.4.1-基于奖励模型的RL-RLHF-PPO.md) 还留着奖励模型和在线策略梯度。[4.4.2](../4.4.2-无奖励模型的对齐DPO-KTO/4.4.2-无奖励模型的对齐DPO-KTO.md) 是离线分类或几率比。这里前几条仍吃成对或 $K$ 路排序，但损失形态都不是 $-\log\sigma(\beta\Delta\log(\pi/\pi_{\mathrm{ref}}))$。PRO 的 $n=2$ 看起来像成对 $\sigma$，对比的仍是长度归一 $\log P$。CPO 看起来像无参考 DPO，还多一项 chosen NLL，实验在翻译上。Nash-MD 是在线的：偏好模型出成对概率，对手是几何混合，求正则 Nash。

SLiC-HF 的校准项是 $\max(0,\delta-\log P(y^+)+\log P(y^-))$，对数似然不除长度；正则是 $-\lambda\log P(y_{\mathrm{ref}})$，不必再驻一份 SFT 做 KL。正负对可以来自本模型采样再打序（sample-rank），也可以直接吃离线人标对（direct）。骨干是 T5，任务是 Reddit TL;DR。

RRHF 的分数是 $p_i=(\sum_t\log\pi(y_{i,t}\mid x,y_{i,<t}))/\lVert y_i\rVert$。hinge 没有间隔，$r$ 只决定哪些对要比、哪一条当冠军，不进 hinge 的数值。$L_{\mathrm{ft}}$ 不除长度。训练期默认一份 $\pi$，需要打分再加一份冻结 $R$。回答可以来自本模型、ChatGPT 或人写，不必绑死当前策略的 rollout。

IPO 仍要 $\pi_{\mathrm{ref}}$。Azar 把 RLHF / DPO 收成 ΨPO 里 $\Psi=\mathrm{logit}$ 的一支；恒等支把最优对数比钉在有界的偏好差上，再用平方损失在成对样本上找根。靶心是 $\tau^{-1}/2$。Hugging Face TRL 的 API 仍把这只旋钮叫 `beta`，那是库名，不是论文字母。实验表不要编 AlpacaEval。

PRO 吃完整 $n$ 长序。分数是长度归一条件对数概率，损失是 $n-1$ 次剩余集合上的 softmax，再对第一名加 $\beta$ SFT。$n=2$ 看起来像成对 $\sigma$，没有 $\pi_{\mathrm{ref}}$。CPO 的主战场是机器翻译：三元组打分只留最高最低，参考策略换成均匀先验，再加 chosen NLL。不要写成「DPO 少画一个参考框」，也不要写成 SimPO。

Nash-MD 的对手是当前策略与 SFT 参考 $\mu$ 的几何混合，不是历史均匀平均。$\beta=0$ 才是对当前自己的 Self-Play，不要和 [05-SPIN](../4.4.2-无奖励模型的对齐DPO-KTO/05-SPIN-自对弈微调/05-SPIN-自对弈微调.md) 的人标对自生成混成一件事。这边的 $\tau$ 是 KL 温度，不要焊到 IPO 的靶心 $\tau^{-1}/2$。

不要把 RRHF 读成「DPO 的 $K$ 路版本」。不要把 IPO 的 $\tau$ 焊成 Rafailov 的 $\beta$。不要把 SLiC 读成「没有正则的纯 hinge」。不要把 PRO 读成 RRHF hinge。不要把 CPO 读成 SimPO。不要把 Nash-MD 读成 SPIN，也不要读成离线 IPO。不要把 Best-of-$n$ 读成 RAFT：RAFT 把 top-1 写进 SFT，BoN 可以不更新权重。

| | 正负怎么进损失 | $\pi_{\mathrm{ref}}$ | 目标形态 |
|--|----------------|----------------------|----------|
| SLiC-HF | 成对，未归一 $\log P$ | 不要（CE 到 $y_{\mathrm{ref}}$） | 有间隔 rank hinge + CE |
| RRHF | $K$ 路，长度归一 $p_i$ | 不要 | 无间隔 hinge + 冠军 SFT |
| IPO | 成对 | 要 | MSE，靶心 $\tau^{-1}/2$ |
| PRO | $n$ 长序，长度归一 $r_\pi$ | 不要 | listwise softmax $+\beta$ 冠军 SFT |
| CPO | 成对，未归一 $\log\pi$ | 不要（均匀先验） | $-\log\sigma(\beta\Delta\log\pi)$ + chosen NLL |
| Nash-MD | 在线 $y$ vs 几何混合 $y'$ | 对手混 $\mu$，KL 也对着 $\mu$ | 正则 Nash，不是 BT 隐式奖励 |
| Best-of-N | 采 $n$ 条，代理 RM 取最高 | 不要（解码选择） | 过优化标度 $R(d)$，不是损失 |
| KTO | 二值单条 | 要（可退化） | $\lambda_y-v$；$z_0=\mathrm{KL}(\pi_\theta\Vert\pi_{\mathrm{ref}})$ |

## 2. 不是谁

**不是** [4.4.1](../4.4.1-基于奖励模型的RL-RLHF-PPO/4.4.1-基于奖励模型的RL-RLHF-PPO.md) 的在线策略梯度。RAFT 只克隆第一名，和 RRHF 丢掉 $L_{\mathrm{rank}}$ 之后才像；序列级 REINFORCE 的滑动平均基线在 [10-REINFORCE](../4.4.1-基于奖励模型的RL-RLHF-PPO/10-REINFORCE-序列级策略梯度/10-REINFORCE-序列级策略梯度.md)。Best-of-$n$ 也可以「采 $n$ 取最高」，但不改参数；过优化标度在 [07](./07-Best-of-N-奖励模型过优化/07-Best-of-N-奖励模型过优化.md)。

**不是** [4.4.2](../4.4.2-无奖励模型的对齐DPO-KTO/4.4.2-无奖励模型的对齐DPO-KTO.md) 的 Bradley-Terry 分类。DPO 的 $Z(x)$ 抵消、ORPO 的几何平均 $P$、SimPO 的长度平均奖励、SPIN 的人标对自生成，都在那一夹。Nash-MD 的目标分布是偏好博弈均衡，不是 $p_{\mathrm{data}}$。

**不是** [4.4.3 RLAIF](../4.4.3-RLAIF/4.4.3-RLAIF.md)：换的是标签来源，附录 E 仍是带价值网络的 REINFORCE。

KTO 曾经在本夹被写成 Forward KL 再减一个阈值，方向反了。$z_0=\mathrm{KL}(\pi_\theta\Vert\pi_{\mathrm{ref}})$，错配估计、不反传。细节只在 03。
